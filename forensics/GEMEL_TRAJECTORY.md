# Gemel trajectory (generated projection)

Generated from the local `.gemel` store by
`forensics/tools/render_gemel_trajectory.sh` inside the FRF tooling
container. Gemel's store is **not** Git-tracked (Gemel ships a
`.gemel/.gitignore` containing `*`), so this projection, Gemel's own
`exchange/` namespace, and the identities quoted below are what travel
in Git. See `docs/DECISIONS.md` D17.

## `gemel log`

```
C76  THE DISPOSITION IS THE ONE THE VOCABULARY ACTUALLY SUPPORTS, AND FINDING THAT OUT REQUIRED READING THE COMPILER. frf residual dispose --help offers eight: fixed, nonreproduced, stabilized, intentional, environmental, oracle_version, harness, unknown. The first choice, nonreproduced, was applied and then REJECTED as a resolution: frf's own claim compiler names the blocking set in source, not in prose -- is_scope_blocking in frf/src/commands/claim.rs matches open | unknown | nonreproduced | stabilized, with the comment that fixed is the ONLY remediation-evidence state and that nonreproduced and stabilized observe disappearance WITHOUT a candidate change, which is 'real evidence of nondeterminism, never a license to claim compatibility'. So a disappearance is not an explanation. Of the remaining three that can unblock, fixed requires a resolution-run under a CHANGED candidate artifact and the candidate never changed (it is the same e4f60d8b throughout, which is the whole point); intentional and oracle_version would both be false statements about what happened. environmental is the one that describes the mechanism: the divergence was produced by an indeterminate stack read inside this project's own probe, courts/phase8/rt_ec_probe.c, which printed an uninitialised one[1]; the value it printed came from the execution envelope and never from either library -- 0 on both sides in the court venue of artifacts/phase8/COURTS.json, 0 against 26 in the FRF venue -- which is why the court venue passed while FRF raised the residual. The residual moved open -> nonreproduced -> environmental, each transition carrying a reason, and the reason states the probe defect and the candidate's unchanged identity outright. THE CORRECTED CLAIM IS EMITTED, AND IT IS STRONGER THAN THE ONE IT REPLACES. frf claim compile --policy sensitivity-backed over the fifteen receipts -- the fourteen unchanged courts plus RT-EC's fresh receipt 6d4aadd1, deliberately NOT the pre-fix c6fc4f0f receipt -- wrote claim 40c229c5c8ea737328f5e57bbcaef223e42f398a1ce4fb0b1499847acb4276d2: authority openssl-rt-3.6.4-r2, candidate openssl-rs 0.0.11 (e4f60d8b), environment x86_64-linux (f41abb27), relation eq(stdout-first-line), eq(exit-code), 15 premises, 0 blockers, 0 excluded evidence. Every premise renders as 'preserves <family> first stdout line and <family> exit class', and RT-EC's is now among them: 'preserves rt-ec first stdout line and rt-ec exit class'. That is the widening C75 measured the cost of and could not pay -- the earlier admissible claim 642866a38904cb4122fa40f345b4a6df181f0439b6b729063349fed203ae107f carried rt-ec bound by its exit class alone, because its premise was dirty and FRF narrowed the cell first. THE PREVIOUS EVIDENCE IS NOT REMOVED, AND NEITHER IS THE DEFECT'S HISTORY. The pre-fix capture, receipt (receipt-run-openssl-rs-rt-ec-dd4ebba9...-c6fc4f0f...) and residual all remain in .frf as the record of what the earlier tooling observed, the C75 change remains in this trajectory with its account of the refusal, and the probe's own header comment records the defect and its fix. The store moved claims 7 -> 8, residuals 168 with the two mutant residuals of the fresh challenge still open by design. WHAT THIS DOES NOT CLAIM: a drop-in replacement. The claim's own non-claims are emitted beside it -- it does not establish byte-identical stderr, full CLI compatibility, or drop-in replacement for all rt-digest behaviour -- and it is a sensitivity-backed claim over fifteen courts' stdout-first-line and exit-class surfaces, which is what forensics/frf/README.md says that policy means.
    state state.57555107463d55cb0f7fc96b5271e03934830b808b9bf01fb38e28b589146443 -> state.763825e5220e475e8a9b11ac183adcf4cede59ed47fe8c22ff97a4bd733efdb6
C75  THE PROBE IS FIXED AND THE ARM IS CLEAN. courts/phase8/rt_ec_probe.c's first version left EC_builtin_curve one[2] uninitialised, so its short.tail arm printed one[1].nid as a sentinel the comment described but the code never set; the court venue of artifacts/phase8/COURTS.json happened to read 0 on both sides and passed, while the FRF venue read 0 against the authority and 26 against the candidate and raised residual e343c18e1484c4fd8394887b6ead5a9797ae6bad6f777d6aa0747df4f15fb808 on RT-EC's stdout surface. C74 recorded that as a defect in the instrument rather than a divergence in the library. The probe now declares EC_builtin_curve one[2] = { { 0, NULL }, { -1, NULL } }; -1 is a NID no builtin curve row carries, so a row the call really had written there could not be mistaken for the sentinel, and the header comment records the whole history. The transcript's short.tail line now reads short.tail=-1,1 on BOTH sides, so the fourth call shape's 'only min(nitems, length) entries are written' claim is observed by construction rather than by accident. RT-EC RE-OBSERVED CLEAN, AND IT TOOK A FRESH IDENTITY. frf court run produced run-openssl-rs-rt-ec-6493159b7743d1b92a3e93c62a3a0889e98e132b66d8f8b4aae41cbd55966162 -- a NEW run id, not the pre-fix dd4ebba9, because the probe binaries in the manifest's execution_context changed -- and printed no residual line, where the pre-fix run printed one before its run line. frf receipt emit produced receipt-run-openssl-rs-rt-ec-6493159b7743d1b92a3e93c62a3a0889e98e132b66d8f8b4aae41cbd55966162-6d4aadd1a5df60afe3e9cd315a81efa4ff0b657e877e412342d5f30b9fbff11c with ***2 observable(s), 0 residual(s) (0 blocking)*** -- zero, as expected. frf court challenge adjudicated both axes on the corrected probe: operator stdout-first-line 4860389ab9470154aad7be157edc5364a14947cbbe7d1cb2e2c667ba90e82ea3 saw the seeded defect on stdout and nothing else, and operator exit-class 5ef6bf7d687f42299b99913c101ee3d8dc5ab59c684bade5acac251c4263c42c saw it on exit and nothing else. The store moved captures 243 -> 246, challenges 164 minus the two adjudications added here -- 162 -> 164, receipts 81 -> 82 and residuals 166 -> 168; claims stays 7. THE RECOMPILE IS REFUSED, AND THAT IS THE FINDING. The sensitivity-backed claim was recompiled over the fifteen receipts -- the fourteen unchanged courts plus RT-EC's FRESH receipt, deliberately NOT the pre-fix rt-ec receipt -- and FRF refused it, verbatim: 'cannot claim compatibility for fixture family rt-digest because residual e343c18e1484c4fd8394887b6ead5a9797ae6bad6f777d6aa0747df4f15fb808 (text) is open -- it was observed on the claimed surface and remains unexplained ... frf: claim refused: 1 blocking residual(s) intersect this claim's scope -- no positive claim emitted while an unexplained divergence on the claimed surface exists'. The mechanism is worth stating precisely, because it is not what a first look suggests: FRF admits a claim over each premise's CLEAN surface, and the corrected receipt's clean surface is now the FULL {stdout, exit}, so the claim's rt-ec cell widened to both axes -- and that widening is what makes the pre-fix residual intersect. The earlier claim 642866a38904cb4122fa40f345b4a6df181f0439b6b729063349fed203ae107f (C74) was admissible only because RT-EC's premise was itself dirty and FRF had narrowed its cell to [exit] first. So a clean receipt makes the claim WORSE, not better, while the pre-fix residual stays open: this change could not emit the both-axes claim the fix was for, and no claim was emitted. THE DISPOSITION IS LEFT TO A HUMAN, AND THE VOCABULARY IS MEASURED. forensics/frf/run_courts.sh states the rule in its own words -- FRF refuses to compile a claim while any residual is open, unknown or harness, so a disposition has to be stated with a reason, and everything except the pre-authorised oracle-version trajectory is left to a human. frf residual dispose --help gives the settable set: fixed, nonreproduced, stabilized, intentional, environmental, oracle_version, harness, unknown. None fits this residual cleanly and each names why: fixed requires a resolution-run under a CHANGED candidate artifact and the candidate library did not change (it is the same e4f60d8b); nonreproduced requires a run under an IDENTICAL candidate, which is true, but it reads the instrument as unchanged when the probe is exactly what changed; and harness is the semantically honest word -- the residual WAS the harness -- and by design it still blocks the claim, which is the framework saying 'fix the harness and re-observe' rather than 'build a claim on this run'. A store recreation would be the project's other precedent (C61 did that for a probe edit), but .frf is committed evidence and this change was instructed to add to it and never to destroy it. So the pre-fix residual e343c18e1484c4fd8394887b6ead5a9797ae6bad6f777d6aa0747df4f15fb808 remains open by design, the admissible claim over this stratum is still 642866a38904cb4122fa40f345b4a6df181f0439b6b729063349fed203ae107f with rt-ec narrowed to its exit axis, and the both-axes claim awaits one disposition stated with a reason: the command is frf --root .frf residual dispose e343c18e1484c4fd8394887b6ead5a9797ae6bad6f777d6aa0747df4f15fb808 --disposition <one of the eight> --reason "<one line>" (plus --observation-run run-openssl-rs-rt-ec-6493159b7743d1b92a3e93c62a3a0889e98e132b66d8f8b4aae41cbd55966162 for nonreproduced), after which the same fifteen-premise compile emits the corrected claim. This change records the correction and the obstacle; it does not choose the disposition.
    state state.047b4200ea7f9b06a4d5ce5d68e326b4d82ee4c076e006146e66e195075529f9 -> state.57555107463d55cb0f7fc96b5271e03934830b808b9bf01fb38e28b589146443
C74  Phase 8 closes. The stratum reads zero open and complete in forensics/phase8-obligations.json -- 786 owned, 786 implemented, 0 deferred, 0 open -- and forensics/phase-state.json derives complete for it with every earlier stratum complete. THE WORKING SET IS THE PRIMITIVES, NOT THE PLUMBING. Phase 7 handed this stratum 27 symbols -- the twelve legacy low-level-key accessors, EVP_PKEY_assign, the EVP_PKEY_encrypt_old/decrypt_old pair, the four EVP_PKEY_get1_{RSA,DH,DSA,EC_KEY} accessors, the three d2i spellings and the three EVP_PKEY_meth_find/get0/get_count readers -- and nine landed subphases discharge them: 8.1 the digest constructions and their OSSL_OP_DIGEST rows (RT-DIGEST, CT-DIGEST), 8.2 the symmetric ciphers (RT-CIPHER, CT-CIPHER), 8.3 the AEAD and mode primitives, 8.4 RSA (RT-RSA), 8.5 DH and DHX (RT-DH), 8.6 DSA (RT-DSA), 8.7 EC (RT-EC), 8.8 crypto/asn1/standard_methods[] and the ASN.1 method objects (RT-AMETH, RT-ECX), 8.9 the pem.h key helpers (RT-PEM-KEY, RT-PUBKEY), and the registration-row courts RT-KEYMGMT, RT-SIGNATURE, RT-ASYM-CIPHER and RT-PROVIDER-CAP that drive the provider rows the earlier passes landed but did not exercise. Every primitive-bearing row carries a second, independent court (D201): a CT-* correctness court is driven by forensics/tools/correctness_vectors.py, which compiles the probe once against the candidate alone and compares its output with committed expected bytes whose provenance is recorded per vector, so an RT-* pass is not cryptographic correctness and a CT-* pass is not OpenSSL parity. EIGHTEEN COURTS, ALL PASS. artifacts/phase8/COURTS.json reads all_pass true over eighteen courts and 15129 authority observations across the fifteen transcript courts, with the three CT-* rows counted as structural. The stratum's provider rows read implemented 306 of 306 owned with 685 handed on. THE FRF CHAIN THIS CHANGE ADDS. Phase 8's one remaining seal gap was docs/RELEASE_GATES.md section 2 items 8 and 10, FRF receipts and a Gemel checkpoint -- the same gap Phase 7 hit and D200 repaired -- and this change repairs it the same way. Fifteen (id, phase, probe, description) rows were added to forensics/tools/gen_frf_courts.py's COURTS table, one per differential court, each naming the artifacts/phase8/probes/<probe>.{authority,candidate} pair the court executes and the courts/phase8/<probe>.c it was compiled from, with the description being the probe header's own one-line subject as the Phase 7 rows carry theirs. python3 forensics/tools/gen_frf_courts.py wrote 154 declaration files for 77 courts and --check moved from ok: 124 file(s) match the table (62 courts) to ok: 154 file(s) match the table (77 courts). The declarations are forensics/frf/courts/openssl-rs-rt-{digest,cipher,cipher-mem,rsa,dh,dsa,ec,ameth,pem-key,pubkey,ecx,keymgmt,signature,asym-cipher,provider-cap}. The chain ran in the FRF tooling container (bash docker/openssl-rs-frf-court.sh up / exec), never on the host, and WITHOUT the rm -rf that forensics/frf/run_courts.sh begins with: .frf is committed evidence and this change adds to it, as D200 did for Phase 7. Fifteen receipts were emitted, one per court; thirty challenge records were produced, both declared axes on every court, every one adjudicated with saw_defect true and specificity_clean true and none refused (D13); and one claim was compiled at --policy sensitivity-backed over the fifteen receipts -- 642866a38904cb4122fa40f345b4a6df181f0439b6b729063349fed203ae107f -- binding authority openssl-rt-3.6.4-r2 and candidate openssl-rs 0.0.11 (e4f60d8b), with zero blockers. The FRF store moved captures 198 -> 243, challenges 132 -> 162, receipts 66 -> 81, claims 6 -> 7 and residuals 135 -> 166; the object closure is complete and the graph verdict is graph_verified. At the next store recreation run_courts.sh's derived RUNTIME_COURTS glob picks these courts up and compiles them into the runtime claim with the Phase 3-7 receipts, so no runner change is needed. THE THREE CORRECTNESS COURTS ARE NOT DECLARABLE, AND THE TABLE SAYS SO. CT-DIGEST, CT-CIPHER and CT-ML-DSA are vector-driven rather than differential: forensics/tools/correctness_vectors.py compiles each probe once against the candidate alone and compares its output with committed expected bytes, so a CT-* court has no authority transcript to diff, no artifacts/phase8/probes/<probe>.{authority,candidate} pair to stage and no {fixture} for a challenge to locate (D13, D201). A manifest generated from the FRF table would name execution-context artifacts that do not exist and would fail at admission, so the table's Phase 8 block declares the fifteen differential courts and records the three-name omission with its reason rather than fabricating rows. THE FINDING, AND IT IS IN THE INSTRUMENT. RT-EC's real run diverged on exactly one observation: short.tail=0,1 on the authority against short.tail=26,1 on the candidate, with every other line of the 2420-line transcript identical. The cause is courts/phase8/rt_ec_probe.c: EC_builtin_curve one[2] is declared at the top of main without an initialiser, EC_get_builtin_curves(one,1) writes min(1,82)=1 row and leaves one[1] untouched, and the probe then prints one[1].nid as a sentinel its own comment says the entry still is but the code never sets. The observation is therefore an uninitialised stack read, deterministic per binary (0 for the authority, 26 for the candidate, stable across repeated runs in this venue) and environment-dependent across venues: artifacts/phase8/COURTS.json's court venue reads 0 on both sides and passes, and the FRF venue reads 0 against 26 and raises one residual. It is the class forensics/tools/probe_hygiene.py exists to catch -- the same defect class the RT-PARAM residual of C61 was -- and not a candidate divergence, so the probe itself must change to be repaired; this change does not edit it, because the change's ground rules forbid editing courts/phase8/*_probe.c, and records it here instead. The claim is honest about the consequence rather than the divergence being smoothed over: FRF admits a claim over each premise's clean surface, so it narrowed the RT-EC premise, and the compiled claim's rt-ec cell reads observable_scope [exit] with the positive assertion 'preserves rt-ec exit class', where the other fourteen premises carry both stdout and exit. RT-EC remains challengeable: its stdout-first-line challenge 5c087f4d2e0393982f3098f74d315e0149958d2b07a827c659262127abce3523 saw the seeded defect on stdout and nothing else, so it is the stdout axis alone that the real run's residual excludes, and the exit-class challenge fe2e39bcfd3e6baf17c96b737a160e0b0f709ab15d715c62c320a71b57824b81 is the one the claim carries. THE DECLARATIONS, THE RECEIPTS, THE CHALLENGE RECORDS, THE CLAIM AND THE CHECKPOINT ARE WHAT THIS CHANGE PRODUCES; the uninitialised read is what it found.
    state state.e46624fe8a76ef4763ba6ae17750bbded15f01b5c80c3628653d4cbcfed07ab4 -> state.047b4200ea7f9b06a4d5ce5d68e326b4d82ee4c076e006146e66e195075529f9
C73  Phase 7 closes. The stratum reads zero open and complete in forensics/phase7-obligations.json, and forensics/phase-state.json derives complete for it with every earlier stratum complete. THE EIGHTY ARE DEFERRALS WITH A CALLEE NAMED. phase7_obligations.py gained BLOCKED_HANDOFFS, a second deferral table beside 7.3g's LEGACY_HANDOFFS: twenty-five groups, one reason each, every reason naming the callee, the authority file and line the call sits on, and the stratum that owns the callee. deferred_to_later_phase moves 164 -> 244 and deferred_by_phase reads {8: 27, 9: 4, 10: 15, 11: 5, 13: 193}. The table fails closed in both directions: a symbol in it that the crate defines is fatal, and so is one that is not in the stratum's working set, so the table is retired by LANDING a name rather than by editing a reason. owning_phase is the LATEST stratum the symbol is blocked on, because that is the first phase whose completion retires the row; where several phases feed one symbol the reason names all of them and says which is binding, which is forensics/tools/phase5_obligations.py's convention (its ASN1_add_stable_module row) rather than the subphase rows' reading, which names the blocker reached FIRST. That distinction is load-bearing for the PEM families: the six readers and nine writers are reached through OSSL_DECODER_CTX_new_for_pkey/OSSL_ENCODER_CTX_new_for_pkey first and cannot be written until PEM_bytes_read_bio/PEM_def_callback exist too. ONE NAME WAS NOT BLOCKED AT ALL. D193's sweep put EVP_PKEY_new_mac_key in the twelve-name legacy-accessor group with the reason that all of them read a legacy key through evp_pkey_get_legacy. Its call list contains no such call: crypto/evp/pmeth_gn.c:313 is EVP_PKEY_CTX_new_id, EVP_PKEY_keygen_init, EVP_PKEY_CTX_set_mac_key, EVP_PKEY_keygen and EVP_PKEY_CTX_free, and D173 had already recorded that the first two gated it -- which stopped being true when EVP_PKEY_CTX_new_id landed (D186) and ctrl_params_translate.c landed (D188). It is implemented in src/evp/pmeth_gn.rs with its thirteen siblings, and RT-EVP-PKEY drives it through an HMAC key type the probe publishes in the DEFAULT library context, because OBJ_nid2sn(EVP_PKEY_HMAC) is HMAC and EVP_PKEY_CTX_new_id passes no property query. The arm observes mac_key.null_engine=nonnull, the generation vector init:1,...gen:1, and saw_priv=1 with priv_len=6 -- the last two are the point, because a transcription that dropped EVP_PKEY_CTX_set_mac_key would still answer non-NULL. RT-EVP-PKEY moves 498 -> 502 and implemented[libcrypto] 1840 -> 1841. TWO MORE NAMES THE STRATUM OWED ITSELF. evp_pkey_decrypt_alloc is implemented: its prerequisite row said 7.4b's operation half would land it directly beside EVP_PKEY_decrypt, that half landed and this did not; it is now in src/evp/asymcipher.rs transcribed from asymcipher.c:332, with the first || arm written as its own if because it returns -1 with *outp untouched, and its prerequisite row is retired. evp_cleanup_int is RETARGETED from this stratum to Phase 8 and not discharged: its row named EVP_PBE_cleanup as the blocker and that landed with 7.4c's PBE remainder (D192), so what is left is the seventh call, evp_app_cleanup_int, which pops the application-supplied EVP_PKEY_METHOD registry. THREE EVIDENCE-LIST ENTRIES NAMED FILES THAT NEVER EXISTED. phase_state.py's phase-7 module list carried src/evp/legacy_cipher.rs, src/evp/legacy_digest.rs and src/evp/params_translate.rs; 7.3g handed the first two families to Phase 13 whole so no file was written, and the ctrl plane landed in src/evp/pkey_ctx.rs (D188). An absent evidence file reads as in-progress, so those three entries are what held the stratum off complete, and the fix is to remove them rather than to create empty modules that satisfy a list. The ledger's MODULE_PREFIXES labels stay: a label is not a claim about a file (D193, D194). FIFTY-SIX UNIT RECORDS, SPLIT 42/14. plan_reconciliation.py judges a unit a COMPLETE stratum's row names, and this plan names fifty-six that are reached or handed on but invisible to its three mechanical signals: 42 deferred_to_later_stratum (31 to Phase 13, 7 to Phase 8, 2 to Phase 10, 1 to Phase 9, 1 to Phase 11) and 14 reached_by_a_named_construct. Each carries the fields its class requires to prove -- owner_phase for a deferral, crate_module and the built names for a named construct -- so a wrong record fails rather than reading plausibly. That is the mechanism forensics/prerequisites.json's own units rule documents for exactly these units, and populating it is what makes the reconciliation read zero AND stay checkable. No change was made to plan_reconciliation.py or to phase7_obligations.py's completion rule; the only machinery edits are the three module-list entries removed from phase_state.py, the seal added to that list, and the seal path added to render_seal_census.py's SEALS table. THE SEAL. docs/PHASE-7-EVP-SEAL.md follows PHASE-6-PROVIDER-SEAL.md and PHASE-5-BN-ASN1-PEM-SEAL.md in shape, cites docs/SEAL-CENSUS.md for every count, copies only the per-court table from artifacts/phase7/COURTS.json, records the ten live and one withdrawn divergences of SECURITY_DIVERGENCE_POLICY.md section 6, carries a one-line register of D139-D196 (D144 and D150 do not exist), and states explicitly that implemented and passing bounded courts are not PARITY_VERIFIED. Phase 7 declares no FRF courts, so it has no FRF receipts, and the seal says so with the file that makes it a convention: gen_frf_courts.py's COURTS table IS the registry of declarations (D58) and its entries are the Phase 3-6 runtime courts, while forensics/frf/run_courts.sh derives what it runs from the declarations directory, so a court with no declaration produces no receipt by construction. gen_frf_courts.py --check prints ok: 86 file(s) match the table (43 courts).
    state state.0ab3c4dabc19701d95bd3e9e3ea9b3eb09bb3b1317f6061a5d85cb016d969672 -> state.e46624fe8a76ef4763ba6ae17750bbded15f01b5c80c3628653d4cbcfed07ab4
C72  6.8c is the provider registry reaches activation, and it is now wholly exported, so it gets a real differential court rather than a slot sweep. provider_core.c sections 947 to 2300 become src/provider/activate.rs and src/provider/stores.rs, alongside the provider_init and ossl_provider_teardown that part 1 landed; the twenty-two exports in src/provider/mod.rs are provider.c whole plus the five provider_core.c wrappers. Phase 6 moves from 117 implemented and 44 open to 139 and 22; libcrypto from 1091 to 1113 of 5896. RT-PROVIDER is the 54th court, 67 observations, no residuals; the five phase court sets total 54 courts and 19,869 observations, all re-derived from the authority in this run. FIVE DEFECTS FOUND, AND ONLY ONE OF THEM BY A COURT. (1) ossl_provider_free flag_initialized arm sat OUTSIDE if (ref == 0), so a provider was torn down on every release instead of the last one. The authority stated reason is the whole argument against that: other structures may still be hanging on to the provider after the last deactivation and may therefore need full access to its services, therefore we deinit late. Nothing could reach the arm when it was written, because ossl_provider_free carried a dead-code allowance; 6.8c exports are what make it live. Its two allocation coordinates also replaced the placeholder zeros that part 1 left: OPENSSL_free of error_strings is at 754 and of operation_bits at 759, so a failing allocation records what a consumer would see from the authority. (2) ossl_provider_add_to_store losing-thread path did not deactivate before freeing, the same function else arm did not flush the decoder cache, and provider_deactivate_free did not consult flag_activated. All three are 6.8c work and all three are now written. (3) RT-PROVIDER FIRST RUN: add_builtin.null_init answered 1 where the authority answers 0. The export took a bare ProviderInitFn, so a NULL entry point became Some(NULL) and ossl_provider_add_builtin NULL test — which the authority performs BEFORE it allocates anything — could not see it. The parameter is now Option<ProviderInitFn>; ABI-PROTOTYPE canonicalises the two identically, because a nullable function pointer and a bare one are the same type to a caller, so the nullable spelling costs nothing and is the one the contract needs. This is the only defect in this subphase that a court found, and it is a real one: a consumer could register a builtin with no entry point and get success. (4) ABI-PROTOTYPE reported type_unmapped 2 for OSSL_PROVIDER_add_builtin and OSSL_PROVIDER_do_all without any court being involved. Both failed on the RUST side, not the C side: the aliases ProviderInitFn and ProviderDoAllFn named their parameters, and a named argument is not a type, so the canonicaliser could not read them. Every function-pointer alias in this crate is spelled unnamed for that reason and these two were not; they are now, and the type plane went from 2 unmapped to 0 with checked rising from 1044 to 1066 declarations and from 1061 to 1083 types. The lesson is that an alias outside every export signature is unchecked until an export uses it. (5) FOUR DEFECTS IN THE INSTRUMENT ITSELF, all found by running it. The probe defined _GNU_SOURCE over the compiler own -D_GNU_SOURCE; OSSL_PARAM_construct_utf8_ptr takes char ** and was handed a char[64]; OSSL_PARAM_END is a brace initializer and not an expression, so assigning it needs OSSL_PARAM_construct_end; and a leftover label from an earlier draft survived into the committed source. And the court then reported that the CANDIDATE had called a SCAFFOLDED symbol, which read as a divergence and was a stale shell: implemented-surface.json had been regenerated to 1113 but build_phase2.sh had not been re-run, so the DSO still carried the old scaffold list and exported a stub for OSSL_PROVIDER_add_builtin. The pipeline order in the handoff says implemented_surface precedes build_phase2 for exactly this reason, and this is what happens when it is not followed. THE FIVE STORE BRIDGES ARE A CHECKED INVARIANT RATHER THAN A STUB. provider_flush_store_cache, provider_remove_store_methods and the two activation functions each end in a call into one of five libctx slots — the four method stores and the decoder cache — and none of the five exists in this build: the stores are created by ossl_method_store_new from Phase 7 and Phase 10 initialisers, and ossl_method_store_cache_flush_all and _remove_all_provided are 6.7c work that 6.7 deferred to 6.8 and has not landed. Every one of the nine delegations is guarded by a NULL test, and the NULL answers are NOT uniform: seven of the nine answer 1, the decoder CACHE answers 0, and the two sums compare against 4. So the part that decides behaviour in this build is the read and the NULL test, and that part is written verbatim; the delegation body is guarded by an assertion on the invariant, the same shape create_provider_children already uses, and a unit test checks the invariant independently so a slot that moves fails the test before the first activation reaches the assertion. Writing a plausible body would be a stub that reads as evidence; writing nothing would let a later stratum fill the slot and silently flush nothing. THE CONSTRUCTION ORDER IS LOAD-BEARING AND THE FIRST TEST GOT IT BACKWARDS. provider_init runs from provider_activate ONLY while the provider has no store, and a second storeless activation re-enters provider_init, finds flag_initialized set, and is refused — non-fatally, because NDEBUG makes ossl_assert into a plain comparison. So OSSL_PROVIDER_try_load_ex order is not a detail: create storeless, activate once while storeless, then add to the store. The test originally added first and expected the count to advance; the failure was the test own. ossl_provider_deactivate is a DEACTIVATION and not a report, and the two conventions are opposite and neither is greater-or-equal-one: provider_deactivate answers the RESULTING count, so from four activations the first call answers 3, and -1 on failure, which is not a count; ossl_provider_deactivate answers a boolean that on the transition to zero IS the store sweep verdict. The test asserted 2 where the wrapper had already consumed one deactivation, which is the instrument before the subject again. WHAT THE PROBE DELIBERATELY DOES NOT OBSERVE, AND WHY. provider_activate_fallbacks loads the table is_fallback rows through their compiled-in entry points, and the candidate has none of them: ossl_default_provider_init is 807 lines and belongs with the algorithm tables in 7 and 8, so provider_init takes the MODULE branch, the DSO_load of libdefault.so fails, and the walk answers 0 where the authority answers 1. The consequence is that doall_activated and OSSL_PROVIDER_available would answer differently if the walk were entered with the flag set, so RT-PROVIDER never puts it in that state: the first public call it makes is OSSL_PROVIDER_load, which sets store use_fallbacks to 0 before it looks anything up. From that point both sides take the walk early return, and available, do_all and the enumeration ARE compared — they would not be otherwise. The disabled path is courted, the enabled path is named rather than hidden, it is recorded as 6.8c residual 1 in the subphase document and in the divergence policy, and a unit test pins the failure so that the day 7 and 8 lands the entry points the test fails and the residual is retired deliberately. THREE SMALLER THINGS KEPT RATHER THAN TIDIED. assert(ref > 0) in ossl_provider_doall_activated is COMPILED OUT under this profile NDEBUG — the authority says so in its own comment, that there is not much to be done if it ever fails — so emitting a live assertion would have been a divergence and not a fidelity. (1u8 << (bitnum % 8)) & 0xFF is kept verbatim from the authority in both bitset functions with an allow naming the lint: the mask is redundant in both languages and it is the line a reader compares against provider_core.c. And D113 rule applied twice more, with the first draft wrong both times: lib_ctx_get_data is a SAFE function in this crate, so the nine unsafe blocks the bridge module was written with were unnecessary, and a raw-pointer comparison needs no block either, so the eight unsafe markers in the test own callbacks became plain comparisons. The rule is not that unsafe functions are everywhere; it is that a safe function called from an unsafe fn needs nothing, and the compiler is the authority on which is which. ONE EVIDENCE-DISCIPLINE FINDING. evidence_determinism and check_evidence_portability both went STALE on the PHASE 3, 4 AND 5 obligation ledgers, because each records the sha256 of the atlas implemented-surface.json among its inputs and that file had changed from 1091 to 1113 implemented symbols. The remedy is the one the tools already prescribe: regenerate and commit, because the generators are the source of truth. Recorded because a ledger that names its inputs is doing its job when it goes stale, and three ledgers going stale at once is the mechanism working and not a defect. The FRF store is recreated: 389 objects, 47 receipts, 94 challenges, 5 claims, 141 captures, 97 residuals, graph_verified.
    state state.3629c0ef2421b29c9860798a6b45ad2e6b862874c2ecc6bc764c37a62b6a7bfe -> state.0ab3c4dabc19701d95bd3e9e3ea9b3eb09bb3b1317f6061a5d85cb016d969672
C71  Slot 1 answers a pointer, and finding the right place to put it turned up a real defect. The plan said 6.8 fills slot 1 and that context_init is where the authority does it. Reading that function to find the position showed the authority builds eighteen slot objects in a documented priority order, and that the CANDIDATE WAS BUILDING THE THREAD SLOT FIRST while the authority builds it SEVENTEENTH. The order is: the seven P2 slots (evp_method_store, provider_conf, drbg, decoder_store, decoder_cache, encoder_store, store_loader_store), all marked 'cleaned up before the provider store'; then provider_store itself, marked P1, 'needs to be freed before the child provider data is freed'; then property_string_data, namemap, property_defns, global_properties, bio_core, drbg_nonce, self_test_cb, indicator_cb, threads, child_provider, property_parse_init, comp_methods. Among the slots this crate has landed the authority therefore builds provider_store FIRST and threads NINTH, and the candidate had threads first and provider_store nowhere. Both context_init and context_deinit_objs now follow the authority for every landed slot, with the unlanded ones kept as gaps rather than compacted, so 6.8e child-provider context and Phase 7/9/10 slots can be inserted at their own positions later. WHY IT MATTERS EVEN THOUGH IT IS NOT DIRECTLY OBSERVABLE: a caller sees only the finished index table, and this profile build sets no-allocfail-tests, so the failure cascade order cannot be exercised either. But context_deinit order IS observable in principle, because a slot object destructor has side effects, and the authority own P1/P2 comments are precisely its statement of those constraints. A deinit order that merely happens to work today is the kind of thing that becomes a defect the moment a destructor gains a side effect, which is exactly what 6.8e will add. SO THIS IS A FIDELITY REPAIR, NOT A REFACTOR: provider_store is released FIRST among the slot objects because the seven ahead of it are explicitly cleaned up before it, and because the child-provider data 6.8e adds must be freed after it. THE COURT SEES IT. RT-LIBCTX model of the slot table moved from 9 filled / 9 deferred to 10 filled / 8 deferred, and it gained two observations (89 to 91) because the probe now names slot 1 in its filled_slots list. It still passes, which is the statement that the authority answers non-NULL for slot 1 too AND agrees on both counts, so the arithmetic and the value are both corroborated rather than asserted. The probe comment was rewritten to say that 1 was filled first rather than appended, because that ordering fact is the finding. ONE AUTHORITY DEFECT NOT REPRODUCED, recorded in the previous subphase and still standing: ossl_provider_new overwrites template.parameters with NULL when the caller supplies params, without freeing a list it may have just deep-copied from the store entry. That leaks; the candidate releases it first. The FRF store is recreated: 381 objects, 46 receipts, 92 challenges, 5 claims, graph_verified.
    state state.8e46899312a320e084c8575e5a11a00b7f171c11ea1428bf283ec9b78c0935c8 -> state.3629c0ef2421b29c9860798a6b45ad2e6b862874c2ecc6bc764c37a62b6a7bfe
C70  The generic dynamic-object layer lands, and it is the first Phase 6 subsystem that is wholly exported, so it gets a real differential court rather than a slot sweep. crypto/dso/dso_lib.c (329 lines) becomes src/dso/mod.rs (983); crypto/dso/dso_dlfcn.c (445) becomes src/dso/dlfcn.rs (616); dso_err.c joins the generated error-coordinate plane (795 to 830 coordinates, 35 of them DSO -- 24 in dso_lib.c and 11 in dso_dlfcn.c); and dso_openssl.c is the one-line accessor that the DSO_DLFCN profile resolves to, so DSO_METHOD_openssl returns the dlfcn method. Phase 6 moves from 102 implemented and 59 open to 117 and 44; libcrypto from 1076 to 1091 of 5896. THIS CHANGE SUPERSEDES C69/K40, which stated the five-phase court total as 19,796 and its pre-6.9 value as 19,651. Both were hand arithmetic and both were wrong: the derived sums are 19,800 and 19,655, and regression_guard prints the same 19,800 from the committed baseline. Nothing about the evidence changed, only the addition, and the correction is recorded here rather than applied quietly because an evidence record that silently acquires the right number is indistinguishable from one that was right all along. RT-DSO is the 53rd court, 145 observations, no residuals; the five phase court sets total 53 courts and 19,800 observations, all re-derived from the authority in this run. TWO ERRORS IN THE INSTRUMENT, BOTH MINE, AND BOTH RECORDED BECAUSE THE SYMPTOMS WERE MISLEADING. First, the court failed its first run on path.query, path.full and path.negative, which differed by exactly the difference between the two libraries path lengths, 79 against 38. That is not a divergence: a DSO subject is a shared library and the two sides are different libraries at different paths, so the path text and its length are the one dimension a DSO court cannot compare. The probe now compares the ARITHMETIC of the size contract instead -- the query is positive, it agrees with the full-size answer, sz in {1,2,4} answers {1,2,4}, a size one below the length truncates to len-1, and the buffer terminates at each size -- and q and full are used only inside relations and never printed. D114 predicted this divergence; the lesson is that the probe had to be built to not MEASURE it rather than merely to tolerate it. Second, and worse: the first version of the <NULL> observation printed data through a helper that ends with ERR_clear_error, and data ALIASES the slot own buffer, which that clear releases. Both sides printed an empty string, which reads as agreement while actually being that the probe never observed anything. The text is now copied before any helper runs. A bug whose symptom is identical-on-both-sides is the symptom of success, which is precisely what the evidence model exists to catch, and it happened inside the instrument. THREE FINDINGS IN THE CODE, ONLY ONE OF WHICH A COURT COULD REACH. (1) ERR_add_error_data APPENDS; it does not replace, and the comment in the code said it did. dlfcn_pathbyaddr was reproduced through ERR_add_error_txt with a note claiming the authority ERR_add_error_data replaces rather than appends, which was written down as the reason for a recorded divergence. Reading crypto/err/err.c settles it the other way: ERR_add_error_vdata reuses the slot existing MALLOCED|STRING buffer, reallocs it to fit and strlcats into it, so both append and the divergence did not exist. A recalled constant presented as a measured one is D33 defect class, and this was its second instance in this stratum. (2) The <NULL> substitution is reachable at exactly one site and was missing. ERR_add_error_vdata does if (arg == NULL) arg = <NULL>, so a NULL argument becomes the literal <NULL>. The candidate c_str_bytes answers an empty slice for NULL, which is invisible at the load and bind sites because those fail immediately after a dlopen or dlsym that just set dlerror state -- but wrong at pathbyaddr, which fails because dladdr did, and dladdr does not touch dlerror. c_str_or_null_literal now makes all three sites match, and the court OBSERVES the text rather than asserting it: ERR_raise(ERR_LIB_USER, 1) puts a code on the queue with no data, the failing DSO_pathbyaddr appends to that slot, and ERR_get_error_all reads back dlfcn_pathbyaddr(): <NULL>, identical on both sides. A mutation that removes the substitution makes the court fail. (3) DSO_bind_func asked the method TWICE where the authority assigns and then tests, so it asks once. dlsym is idempotent and raises nothing OpenSSL-owned, so no court could have found this; it was found by reading the authority function beside the candidate, which is why that reading is part of the method. FOUR BEHAVIOURS REPRODUCED BECAUSE THEY LOOK LIKE MISTAKES. DSO_ctrl with SET_FLAGS and -1 answers 0 and then reads back -1 with a CLEAN queue, so the authority own comment that a negative answer means an error does not hold for the value a caller may have stored. DSO_convert_filename translates libfoo.so to liblibfoo.so.so -- D114 predicted liblibfoo.so, and the extension is appended to what is already there. DSO_merge refuses a NULL first spec BEFORE it reads the no-translation flag, so the merger own both-NULL arm is unreachable through DSO_merge: it needs a non-NULL first spec to be entered at all and then its condition cannot hold, so it is dead code in both. And DSO_load refusals are ordered: the already-loaded test comes before the filename is looked at, so a second load on a live object is DSO_R_DSO_ALREADY_LOADED and leaves the caller object untouched -- observed by binding through the object after the refusal, not merely by the return code. ONE CI-ADJACENT FIX. RT-DSO is the first probe that cannot be compiled without per-side input: it needs the path of the library under test, because the only DSO_load both sides can be expected to succeed at is a load of the library each is itself built as. probe_hygiene compiles every probe independently at three optimisation levels and reported UNSTABLE, correctly, via the probe own #error. The fix is not a definition table in the hygiene tool: it imports each phaseN_courts.py, finds the one that lists this probe, and calls its extra_defs. That follows discover_probes reasoning, and it means adding a Phase 7 court with a definition does not require remembering a second place. The definition is never printed by the probe: the observations around it are NULL-ness and strcmp against the input, which are equal on both sides by construction. The FRF store is recreated: 381 objects, 46 receipts, 92 challenges, 5 claims, graph_verified.
    state state.b506e9e1c63eb063c238d03d0eb5f546fb1254326532f15e127c7fd1e65c4d51 -> state.8e46899312a320e084c8575e5a11a00b7f171c11ea1428bf283ec9b78c0935c8
C69  The generic dynamic-object layer lands, and it is the first Phase 6 subsystem that is wholly exported, so it gets a real differential court rather than a slot sweep. crypto/dso/dso_lib.c (329 lines) becomes src/dso/mod.rs (983); crypto/dso/dso_dlfcn.c (445) becomes src/dso/dlfcn.rs (616); dso_err.c joins the generated error-coordinate plane (795 to 830 coordinates, 35 of them DSO); and dso_openssl.c is the one-line accessor that the DSO_DLFCN profile resolves to, so DSO_METHOD_openssl returns the dlfcn method. Phase 6 moves from 102 implemented and 59 open to 117 and 44; libcrypto from 1076 to 1091 of 5896. RT-DSO is the 53rd court, 145 observations, no residuals; the five phase court sets total 53 courts and 19,796 observations, all re-derived from the authority in this run. TWO ERRORS IN THE INSTRUMENT, BOTH MINE, AND BOTH RECORDED BECAUSE THE SYMPTOMS WERE MISLEADING. First, the court failed its first run on path.query, path.full and path.negative, which differed by exactly the difference between the two libraries path lengths, 79 against 38. That is not a divergence: a DSO subject is a shared library and the two sides are different libraries at different paths, so the path text and its length are the one dimension a DSO court cannot compare. The probe now compares the ARITHMETIC of the size contract instead -- the query is positive, it agrees with the full-size answer, sz in {1,2,4} answers {1,2,4}, a size one below the length truncates to len-1, and the buffer terminates at each size -- and q and full are used only inside relations and never printed. D114 predicted this divergence; the lesson is that the probe had to be built to not MEASURE it rather than merely to tolerate it. Second, and worse: the first version of the <NULL> observation printed data through a helper that ends with ERR_clear_error, and data ALIASES the slot own buffer, which that clear releases. Both sides printed an empty string, which reads as agreement while actually being that the probe never observed anything. The text is now copied before any helper runs. A bug whose symptom is identical-on-both-sides is the symptom of success, which is precisely what the evidence model exists to catch, and it happened inside the instrument. THREE FINDINGS IN THE CODE, ONLY ONE OF WHICH A COURT COULD REACH. (1) ERR_add_error_data APPENDS; it does not replace, and the comment in the code said it did. dlfcn_pathbyaddr was reproduced through ERR_add_error_txt with a note claiming the authority ERR_add_error_data replaces rather than appends, which was written down as the reason for a recorded divergence. Reading crypto/err/err.c settles it the other way: ERR_add_error_vdata reuses the slot existing MALLOCED|STRING buffer, reallocs it to fit and strlcats into it, so both append and the divergence did not exist. A recalled constant presented as a measured one is D33 defect class, and this was its second instance in this stratum. (2) The <NULL> substitution is reachable at exactly one site and was missing. ERR_add_error_vdata does if (arg == NULL) arg = <NULL>, so a NULL argument becomes the literal <NULL>. The candidate c_str_bytes answers an empty slice for NULL, which is invisible at the load and bind sites because those fail immediately after a dlopen or dlsym that just set dlerror state -- but wrong at pathbyaddr, which fails because dladdr did, and dladdr does not touch dlerror. c_str_or_null_literal now makes all three sites match, and the court OBSERVES the text rather than asserting it: ERR_raise(ERR_LIB_USER, 1) puts a code on the queue with no data, the failing DSO_pathbyaddr appends to that slot, and ERR_get_error_all reads back dlfcn_pathbyaddr(): <NULL>, identical on both sides. A mutation that removes the substitution makes the court fail. (3) DSO_bind_func asked the method TWICE where the authority assigns and then tests, so it asks once. dlsym is idempotent and raises nothing OpenSSL-owned, so no court could have found this; it was found by reading the authority function beside the candidate, which is why that reading is part of the method. FOUR BEHAVIOURS REPRODUCED BECAUSE THEY LOOK LIKE MISTAKES. DSO_ctrl with SET_FLAGS and -1 answers 0 and then reads back -1 with a CLEAN queue, so the authority own comment that a negative answer means an error does not hold for the value a caller may have stored. DSO_convert_filename translates libfoo.so to liblibfoo.so.so -- D114 predicted liblibfoo.so, and the extension is appended to what is already there. DSO_merge refuses a NULL first spec BEFORE it reads the no-translation flag, so the merger own both-NULL arm is unreachable through DSO_merge: it needs a non-NULL first spec to be entered at all and then its condition cannot hold, so it is dead code in both. And DSO_load refusals are ordered: the already-loaded test comes before the filename is looked at, so a second load on a live object is DSO_R_DSO_ALREADY_LOADED and leaves the caller object untouched -- observed by binding through the object after the refusal, not merely by the return code. ONE CI-ADJACENT FIX. RT-DSO is the first probe that cannot be compiled without per-side input: it needs the path of the library under test, because the only DSO_load both sides can be expected to succeed at is a load of the library each is itself built as. probe_hygiene compiles every probe independently at three optimisation levels and reported UNSTABLE, correctly, via the probe own #error. The fix is not a definition table in the hygiene tool: it imports each phaseN_courts.py, finds the one that lists this probe, and calls its extra_defs. That follows discover_probes reasoning, and it means adding a Phase 7 court with a definition does not require remembering a second place. The definition is never printed by the probe: the observations around it are NULL-ness and strcmp against the input, which are equal on both sides by construction. The FRF store is recreated: 381 objects, 46 receipts, 92 challenges, 5 claims, graph_verified.
    state state.a663dbc0d8eea9cc60532a27f37d8b0219341ad986bc2e060016627d63814284 -> state.b506e9e1c63eb063c238d03d0eb5f546fb1254326532f15e127c7fd1e65c4d51
C68  The property engine's grammars close. crypto/property/property_parse.c (763 lines) as src/property/parse.rs, property_query.c (80) as src/property/query.rs, all of defn_cache.c, the list and definition types as src/property/list.rs, and two supporting mirrors: crypto/bsearch.c as src/runtime/bsearch.rs, and the authority's ctype_char_map[128] GENERATED from crypto/ctype.c by gen_ctype_table.py. Unit tests 196 to 216; no export changes, so Phase 6 stays at 102 implemented and 59 open and no court moves. THREE THINGS ARE REPRODUCED BECAUSE THEY LOOK LIKE MISTAKES. (1) The reverse printer walks the sorted array BACKWARDS: ossl_property_list_to_string starts at properties[num-1] and decrements while the output cursor advances, so the string lists clauses in DESCENDING name_idx order even though the array is sorted ascending. The test does not assert the oddity in the abstract -- it uses 'input=certificate,output=certificate', where ossl_property_parse_init interns output BEFORE input, so the higher index is input and the printed string must contain input first; a forward walk would fail. (2) ossl_property_list_to_string's put_num advances the cursor by a length it computed ITSELF rather than by what it wrote, so a truncated number leaves the cursor PAST the NUL that terminated it and the next writer overwrites that NUL. And a buffer with exactly one byte of room becomes a terminator rather than a character, which is the *remain == 1 arm of put_char. (3) put_str reads its input TWICE: the first pass decides only WHICH quote (single, or double when the string contains a single) and never whether the value will be truncated; a restructured single-pass version would agree on every short value and differ on a value containing an apostrophe that also overflows the buffer. THE DEFINITION CACHE HAS FOUR SHAPES, NOT TWO. prop == NULL answers 1 without touching anything; pl == NULL DELETES the entry and answers 1; an already-cached text frees the caller's list and OVERWRITES *pl with the cache's own, so both sides then share one object and nothing may free it twice; and otherwise the text and the list are copied into a single self-referential block whose key lives inside it. Three tests cover all four plus per-context isolation, and the pointer identity the third shape depends on is asserted rather than assumed. ossl_assert IS NON-FATAL HERE TOO: NDEBUG is defined in the admitted build, so the two asserts in ossl_prop_defn_get are (x) != 0 and a NULL table answers NULL rather than aborting -- the same build fact D109 read from configdata.pm, now applied to a third file. TWO GENERATOR FINDINGS. First, the first version of gen_ctype_table.py read ONE line per table entry, and the authority wraps each entry's sum onto a continuation line, so every mask after the first line was dropped and xdigit came out with 0 members where the authority has 22. 'The table has no xdigit bits' is exactly the shape of a real finding and it was not one: the instrument was wrong before the authority was. The corrected counts are 10/22/6/52/62/95 and the parser now checks each comment against its position. Second, and more useful: Phase 5's five ctype predicates recorded their equivalence to the authority's TABLE in a doc comment and NOTHING checked it. One test now walks -256..512 -- the values either side of the 128-byte boundary included -- and asserts all seven predicates agree with the generated table. They do, and the claim is retired into evidence. That is also why the table is generated rather than written: Phase 5's own header refuses 128 hand-copied masks, and a generated table is the opposite of that. A CI FAILURE AND ITS FIX. The determinism tool runs every listed generator, and gen_ctype_table.py reads crypto/ctype.c -- which is NOT COMMITTED, because the authority's source tree is 100 MB and lives only in the court. That is also the explanation for the gap D109 recorded as unexplained, that gen_err_raise_sites.py was never in the list. The generator now has two tiers and PRINTS WHICH ONE RAN: with the authority present it re-derives from the source (the strong tier, which is what the court and the whole pipeline use); with it absent it checks the generated Rust against the committed artefact's own recorded table, which still catches a hand-edited ctype_table.rs. Both tiers were exercised, including a sensitivity control: appending one byte to ctype_table.rs makes the strong tier fail with 'has drifted from the authority'. A CLIPPY VERDICT THAT CORRECTS AN ASSUMPTION. The crate's convention -- every pointer operation in its own unsafe block with a SAFETY line directly above -- does not mean every CALL in an unsafe fn needs one: lib_ctx_get_data, lib_ctx_read_lock, lib_ctx_write_lock, lib_ctx_unlock and CRYPTO_malloc are SAFE functions in this crate, so wrapping them is an unused_unsafe error under -D warnings. Eight such blocks came out of defn_cache.rs and one out of put_char, where a pointer cast needed no block either. Also caught by clippy rather than by a court: items_after_test_module (the printer was appended after the cfg(test) module, so the module moved to the end) and expect_used (denied crate-wide, so three expects became unwrap_or_default/unwrap_or with assertions that carry the transcript). Every symbol in the new modules carries an allow(dead_code) naming 6.8: the whole subsystem is the interface the provider fetch is written against and nothing calls it yet. The FRF store is recreated: 45 receipts, 90 challenges, 5 claims, 373 objects, graph_verified.
    state state.d147184cef695c0a31f9068eb91507ab19cc3ab580c0c46e4b10bc08ebb2894d -> state.a663dbc0d8eea9cc60532a27f37d8b0219341ad986bc2e060016627d63814284
C67  The property engine's first stratum lands: crypto/property/property_string.c as src/property/strings.rs, plus defn_cache.c's constructor and releaser, plus property.c's two global-property functions, plus ossl_property_parse_init from property_parse.c. Index slots 2, 3 and 14 are filled, so nine of the eighteen are live, and context_init now ends with the property pre-initialisation the authority puts there. No export changes: the Phase 6 ledger stays at 102 implemented and 59 open. THE CORRECTION, and it is to the plan rather than to the code. docs/PHASE-6-SUBPHASES.md recorded 6.7's exit criterion as 'definition, parse, string round-trip, matching, query parse and negative selection', with RT-PROPERTY as its court. Measured against the authority, THE PROPERTY ENGINE HAS NO EXPORTED SYMBOL AT ALL: all thirty-odd entry points are ossl_property_*, ossl_ctx_global_properties* and ossl_prop_defn_*. A probe is compiled against INSTALLED headers and linked against the library, so it cannot call one function of the grammar, and a court claiming to compare the parse would be claiming an observation no probe can make -- the failure mode this project has recorded four times in other forms (a2d_ASN1_OBJECT, the prefix-derived discovery of D49 and D51, the unread ledger of D94). The criterion and the court are corrected in place, and the BEHAVIOUR it named is not dropped: it becomes observable at 6.8, where a provider fetch applies a query to a candidate set, and is corroborated at 6.12 by an independently written provider. That is PROVIDER_MODEL.md section 5's gate item 4, 'property-based fetch selection matches, including negative selection', and it was always 6.8's. WHAT THE THREE SLOTS CAN WITNESS: RT-LIBCTX gains slots 2, 3 and 14 and moves 83 to 89 observations -- presence, stability, per-context distinctness and difference from the object's own address, for each of the three, on two independent contexts. THE ORDERING CONTRACT IS NOT VISIBLE THROUGH A SLOT, but it is asserted by the authority at every context construction and reproduced here: ossl_property_value(ctx, 'yes', 1) must be OSSL_PROPERTY_TRUE (1) and ossl_property_value(ctx, 'no', 1) must be OSSL_PROPERTY_FALSE (2). The value table's counter is SEPARATE from the name table's, so the six predefined names take 1..6 in their own space while yes and no are the FIRST TWO VALUES. One shared counter, or interning the names as values, fails the authority's own check. The two ifs are written as two ifs rather than one tuple comparison because the authority's || short-circuits: a table that numbers yes wrongly also leaves no uninterned, and that state is part of what is reproduced. Three unit tests assert it, because no court can: the reference is treated as a contract that has to be made where it can be. TWO SLOTS ARE FILLED AND EMPTY, AND THAT IS THE AUTHORITY'S OWN STATE. ossl_property_defns_new is one empty lhash and ossl_ctx_global_properties_new is one OPENSSL_zalloced block with a NULL list. A zeroed holder is a VALID EMPTY holder, not an uninitialised slot: the authority's own constructor produces exactly this, and the reader that would distinguish them returns &globp->list. So filling these two asserts nothing about property behaviour, and the releasers handle a non-empty state because that is the state 6.7b will create -- a releaser that ignored the list would leak. THE ELEMENT LAYOUT IS REPRODUCED, NOT APPROXIMATED. PROPERTY_STRING is { const char *s; OSSL_PROPERTY_IDX idx; char body[1]; } with s pointing at its own body, so one allocation holds the header and the string and property_free is a bare OPENSSL_free. On x86-64 that is size_of 16 with body at offset 12, and new_property_string allocates 16 + l. A Rust repr(C) type with a trailing [c_char; 1] gives the same offsets and the same size_of, so the arithmetic is the authority's down to the slack byte. defn_cache.c's element has the same shape and the same treatment. PROP_R_* IS THE FIRST REASON FAMILY IN THIS TABLE THAT IS NOT IN AN INSTALLED HEADER. The error-coordinate resolver had only ever needed installed headers -- ERR_R_*, CRYPTO_R_*, BIO_R_*, ASN1_R_*. The property grammar raises PROP_R_*, which lives in include/internal/propertyerr.h, present in the committed source tree and NOT installed. The resolver therefore gained -I <authority source>/include as a SECOND include directory, after the built prefix, so every openssl/... header still comes from the prefix the courts link against and only internal/... falls through to the tree the build was made from. 23 coordinates were added -- property_string.c's 3 and property_parse.c's 20 -- taking the table from 772 to 795. property_parse.c's are 6.7b's implementation and are taken now because the rule Phase 4 and Phase 5 established is the SUBSYSTEM set rather than the implemented subset: a site nobody calls yet is a coordinate, not a claim. A NAMING CORRECTION: the file mirroring property.c was first written as src/property/property.rs, which clippy refuses (module_inception) and which would have needed an allow -- a suppression rather than a correction. It is src/property/globals.rs, named for the object, and says in its header that everything in it is property.c's. The FRF store is recreated: 45 receipts, 90 challenges, 5 claims, 373 objects, graph_verified.
    state state.0ae0259b09b7d22f8ef851f646873124546ba002881d80818d831e118b4cc004 -> state.d147184cef695c0a31f9068eb91507ab19cc3ab580c0c46e4b10bc08ebb2894d
C66  The core BIO lands: crypto/bio/bss_core.c (188 lines) as src/context/core_bio.rs plus src/context/dispatch.rs for the OSSL_DISPATCH walk, OSSL_LIB_CTX_BIO_CORE_INDEX (17), and the export that index exists for. Three exports are implemented -- BIO_s_core, BIO_new_from_core_bio and OSSL_LIB_CTX_new_from_dispatch -- and slot 17 is filled, so six of the eighteen index slots are live. Phase 4's seal named this subphase as the obligation its accepted-and-ignored BIO_new_ex libctx argument created, so src/runtime/bio/mod.rs's Bio gains a libctx field here; that is a later stratum changing a sealed one's source, which the seal rules permit only because the earlier seal named the obligation. The Phase 4 courts are unmoved at 3,244 observations, because no Phase 4 behaviour reads the field. THE FINDING: slot 17 is filled EAGERLY. context_init calls ossl_bio_core_globals_new for every context, so OSSL_LIB_CTX_get_data(ctx, 17) answers non-NULL for a context that has never seen a dispatch table. The consequence is not cosmetic: get_globals CANNOT return NULL through the public API, so BIO_new_from_core_bio's NULL answer comes from the absent BIO_read_ex and BIO_write_ex callbacks and never from an absent globals block, and the if (bcgbl == NULL) arms in all seven operations are unreachable from a consumer. A candidate that built the globals lazily would answer every constructor observation identically and still be wrong about the slot. RT-LIBCTX's filled_slots gained index 17 -- the same fact from the other end -- and moves from 81 to 83 observations. Second finding: bio->libctx is stored AS GIVEN and resolved at USE time. The authority's BIO_new_ex assigns the argument with no concretisation, and ossl_lib_ctx_get_concrete(NULL) answers the THREAD default, so a core BIO built with a NULL context reaches whatever this thread's default is when the operation runs. RT-BIO-CORE proves it rather than asserting it: it builds one such BIO before any default is installed and writes to it (0, no callbacks reachable), then installs a context that has them and writes to the SAME BIO again (18, that channel's answer). A candidate that resolved the context at construction would pass every other observation in this court and fail exactly that one. The court is a matrix over DELIBERATELY INCOMPLETE tables: BIO_new_from_core_bio accepts a table carrying only one of read_ex and write_ex, so the probe builds a write-only and a read-only table and observes each missing callback separately -- read_ex and write_ex answer 0, ctrl, gets and puts answer -1, destroy answers 0. A transcription that picked one of the two values and used it everywhere passes a happy-path test and fails this one. The write-only table also carries TWO write_ex entries, because the walk arms a slot only while it is empty: the first entry wins and the second is never reached, which turns the internal function's first-table-wins rule into something observable through an export. The handle the callbacks receive is compared against the constructor's argument and against NULL and only the booleans are printed -- an address would make the transcript a property of the loader. Two contexts holding two tables are named the same way: the callbacks answer 3 times their channel number, so the transcript says which channel ran without trusting a counter. 109 observations, zero residuals, first run, no defect found. THREE AUTHORITY FAULT BOUNDARIES, REGISTERED RATHER THAN REPRODUCED. Each calls a stored function pointer with no NULL test. D-BIOCORE-1: a table with read_ex or write_ex but no BIO_up_ref -- the guard passes and the NEXT LINE jumps to zero. D-BIOCORE-2: BIO_free of a core BIO whose context has no BIO_free callback, which is every BIO built as BIO_new(BIO_s_core()). D-BIOCORE-3: OSSL_LIB_CTX_new_from_dispatch(handle, NULL), which is reachable from an EXPORT because the export forwards its table straight into the walk whose own loop condition dereferences it. The candidate's slots are Option<fn> precisely so that absence is representable, and each such path answers the documented failure instead; no caller supplying a usable table can tell the two behaviours apart, because in the authority every path that reaches the unguarded call dies there. THE PROBE WAS THE SUSPECT TWICE BEFORE THE CRATE WAS. BIO_method_type and BIO_method_name take a const BIO *, not a BIO_METHOD *, so the first draft compiled against a type error it read as a warning; and a channel's seven callbacks are defined whichever table it sits in, so the two deliberately incomplete tables left four and five functions unreferenced. Both were the instrument rather than the subject. The second also produced a unit-test trap worth naming: clippy rejects a SAFETY comment that is not DIRECTLY above its unsafe block -- an assert_eq! between them does not count -- and it rejects panic! in a cfg(test) module too. The authority's file is crypto/bio/bss_core.c, not the bio_core.c docs/PHASE-6-SUBPHASES.md had recorded, corrected there. The FRF store is recreated: 45 receipts, 90 challenges, 5 claims, 373 objects, graph_verified.
    state state.b540f7ab44c3e9e622e85c2fa981110f901ade7885d7eb5f101033dd1664f29f -> state.0ae0259b09b7d22f8ef851f646873124546ba002881d80818d831e118b4cc004
C65  The name map lands: crypto/core_namemap.c, 584 lines, as src/context/namemap.rs -- all ten internal functions over a real STACK_OF(NAMES). It adds NO export, so what moves is slot 4: RT-LIBCTX goes from 79 to 81 observations, comparing the slot's presence, stability, per-context distinctness and difference from the object's own address. Five of the eighteen index slots are filled now. Three behaviours are the ones a plausible reading gets wrong. (1) The comparison is a BIT MASK, not a case fold: ossl_ht_strcase is tgt[i] = ~0x20 & src[i], which folds case for ASCII letters and does something else for every other byte -- '!' (0x21) and 0x01 both map to 0x01, so two names differing only by bit 5 in a non-letter position are the same name to this map. Reproduced rather than normalised. The same macro caps the key at 63 bytes, so two names sharing a 63-byte prefix are one name; the explicit-length form keys on a prefix instead. (2) NDEBUG is defined in the admitted build -- read from the authority's own configdata.pm -- and that decides a branch: the two ossl_asserts here are OPENSSL_die in a debug build and a plain (x) != 0 under NDEBUG, so a NULL namemap REACHES the ERR_R_PASSED_NULL_PARAMETER raise rather than aborting the process. The opposite assumption turns a raise into a process death. (3) The conflict refusal is ORDER-DEPENDENT, and the first run of the unit test found it: the test asserted that add_names(nm, 0, "fresh:known", ':') is refused because 'known' belongs to another number, and it FAILED because the call is ACCEPTED -- the number being built starts as the caller's, a part only sets it when that part resolves to an existing number, so 'fresh' resolves to 0 and changes nothing, 'known' then resolves to its number, and no comparison against 'fresh' ever happens. The conflict is reachable only between a part that comes after one that already resolved. Both halves are asserted now and the behaviour is documented, because it is what a later reader would otherwise 'fix'. Also reproduced: max_number is stored on every successful addition, so appending an alias can LOWER it -- the only reader asks whether it is zero, so the oddity is invisible and still reproduced; and the stored flag makes ossl_namemap_free a no-op, with the context's destructor clearing it first. The legacy pre-population is deferred WHOLE: running the four RSA-PSS aliases alone would make the emptiness guard false, and a later phase adding the legacy load would then skip it entirely, silently leaving the legacy names out of a map already numbered wrongly. The container is not the authority's hashtable -- lookups are identical and the CRYPTO_R_TOO_MANY_NAMES collision failure is unreachable, recorded as a residual with one allow(dead_code) function that says why a raise which cannot happen is not called. crypto/core_namemap.c is now in gen_err_raise_sites.py's covered set, and regeneration was purely additive: 767 sites to 772, nothing removed or changed. One of the five is the first site in the table whose reason is chosen at run time, and the generator classified it dynamic_reason rather than guessing. A gap this exposed is recorded rather than fixed here: err_sites.rs and err-raise-sites.json are generated but are in neither evidence_determinism.py's generator list nor its comparison list, so CI would not notice stale coordinates -- and they are contract, since ERR_get_error_all reports them. The FRF store is recreated: 44 receipts, 88 challenges, 5 claims, graph_verified.
    state state.412c476ce38dc12187f435df98bc50405ca8de34b3766952768ae734198cdd45 -> state.b540f7ab44c3e9e622e85c2fa981110f901ade7885d7eb5f101033dd1664f29f
C64  All nine exports of self_test.h and indicator.h land in src/selftest/{mod,indicator}.rs from crypto/self_test_core.c and crypto/indicator_core.c. RT-SELFTEST observes them in 71 observations with zero residuals on the first run, and slots 12 and 22 join RT-LIBCTX's filled_slots list, taking that court from 75 to 79. The probe IS the callback, because both surfaces are callback plumbing and there is nothing else to observe; four things came out of that. (1) The array's entries ALIAS the object's own fields: self_test_setparams builds st-phase/st-type/st-desc with OSSL_PARAM_construct_utf8_string(key, st->field, 0), which stores the ADDRESS of the field rather than a copy, and OSSL_SELF_TEST_onend reassigns all three fields to None AFTER calling the callback without rebuilding the array -- so the same array reports Pass inside the callback and None afterwards. The probe stashes the array pointer inside the callback and reads through it again after the call, which is the only way a C caller can see the aliasing at all, and it is why this court is worth writing rather than unit-testing. (2) onend treats anything other than 1 as a failure, including 0 and negative values. (3) oncorrupt_byte's answer is the callback's inverted: the callback answering 0 flips the first byte and the call answers 1. (4) The object's callback is the one passed to OSSL_SELF_TEST_new, not the context's, so the probe sets them to different things and checks which one ran -- an implementation that read the context's callback from inside onbegin would pass a probe that only ever set both to the same function. The prototype court found a real defect the runtime court could not: an earlier revision declared OSSL_SELF_TEST_get_callback's second parameter as *mut *mut c_void, but the authority's is OSSL_CALLBACK **, a pointer to a FUNCTION POINTER. Both spellings work at run time for any caller that passes correctly sized storage, so RT-SELFTEST passed 71 observations against the wrong prototype while ABI-PROTOTYPE reported it as one of two type mismatches and named the canonical form; OSSL_INDICATOR_get_callback had the same defect. That is the blind spot D98 and D103 each recorded from the other direction: a runtime court compares answers, and a prototype wrong only in the SHAPE of an output parameter has no answer to compare. Two safety divergences are recorded rather than reproduced: oncorrupt_byte with a NULL bytes and a refusing callback answers 0 where the authority writes through the NULL, and both setters store nothing and both getters answer NULL when the slot cannot be read. The indicator half is storage without a caller, and that is stated rather than implied: nothing invokes the indicator callback, because the code that reports an indicator is provider-side. The FRF store is recreated: 44 receipts, 88 challenges, 5 claims, graph_verified.
    state state.0d74316188bac1d3fa066deb9e32ce6859deceee93435daa9339bf8ea0048f30 -> state.412c476ce38dc12187f435df98bc50405ca8de34b3766952768ae734198cdd45
C63  Slot 19 of the library context lands, with the two accessors that read it. The slot is crypto/thread/internal.c's ossl_threads_ctx_new/_free -- two counters, a mutex and a condition variable -- created by context_init, which now fails the whole context if the slot cannot be built, and released by context_deinit_objs in the authority's OPENSSL_NO_THREAD_POOL position between the two callback slots and child_provider. RT-THREADDATA observes it in 39 observations with zero residuals on the first run, and RT-LIBCTX grows from 73 to 75 as slot 19 joins its filled_slots list, which is the mechanism 6.6a described for exactly this. The two accessors look trivial and are not: everything about them is in the plural. The counter is per CONTEXT, so setting it on one must not move another's and the default context is one of the contexts rather than a special case; a NULL context resolves through the library context default chain, so the same call answers differently once this thread installs a default, which the probe observes by installing one, reading through NULL, restoring and reading again; and the value is stored verbatim with no range check, so UINT64_MAX is legal to set and to read back and zero is a value rather than unset -- a uint64_t that was clipped, or a setter that refused a large value, would be a plausible reading and a wrong one. The pool's primitives come with it because ossl_threads_ctx_new allocates a mutex and a condition variable and FAILS if either cannot be built, so they are the constructor's and cannot be deferred with it. src/runtime/thread.rs gains ossl_crypto_mutex_* and ossl_crypto_condvar_* over the same primitives, with two design points recorded: the mutex is not recursive, because PTHREAD_MUTEX_DEFAULT deadlocks on re-lock and std::sync::Mutex would instead error or panic, so it parks on a flag and deadlocks as the authority does; and a condition variable is paired with one mutex with the pairing made explicit, bound on first wait, because Rust's Condvar::wait needs the guard of the mutex it waits on while the C API creates the two independently -- every use in the authority pairs one condvar with one mutex for its lifetime, so this makes an invariant the authority relies on explicit and a mismatched pair is reported rather than becoming a lost wakeup. Nothing waits on the condition variable yet: it is created and released because the constructor creates and releases it. 6.6e is split, and the reason is a file rather than a distance: the counter pair needs only the slot, but OPENSSL_thread_stop/_ex need crypto/initthread.c's per-thread event-handler table and OPENSSL_atexit needs DSO_dsobyaddr to pin its handler's object, so those three become 6.6e-ii with both dependencies named and context_deinit's missing ossl_ctx_thread_stop call is the line it unblocks. The FRF store is recreated: 43 receipts, 86 challenges, 5 claims, graph_verified.
    state state.5856f8075027b3886decc069f9bdb4e7538257aaa1ba8f14bb00dcac5ae826e4 -> state.0d74316188bac1d3fa066deb9e32ce6859deceee93435daa9339bf8ea0048f30
C62  OSSL_LIB_CTX lands with the seven of its ten exports that need nothing else: new, free, get0_global_default, set0_default, get_data, get_conf_diagnostics, set_conf_diagnostics. RT-LIBCTX observes all of it -- 73 observations, zero residuals, first run -- covering three behaviours that are in no documentation: the default chain, in which a NULL context resolves to this thread's slot and OSSL_LIB_CTX_set0_default returns the PREVIOUS default while installing a new one; the free rules, where two of the three arguments are no-ops (NULL, and whatever the calling thread's default resolves to, which includes the global default and any context the thread installed itself) so a caller that frees its own thread default and keeps using it is not reading released memory; and the index registry, whose shape a caller can see through the exported function alone. The probe was run against the authority before any Rust was written, and the reconnaissance changed the design: the switch answers a pointer for eighteen indices and NULL for 7, 8, 9, 13 and everything past 22, index 19 answers a pointer (so this profile has the thread pool compiled in), and index 21 answers the ADDRESS OF A FIELD inside the object, so it is non-NULL for a context whose compression stack is empty -- which is also the arm most likely to be simplified into returning the field's value and answering NULL for every context, so it has a unit test of its own. Three of the ten exports are not writable yet and the split is by dependency, not convenience: new_from_dispatch needs the core BIO (6.6c), new_child needs ossl_provider_init_as_child (6.8) and is what sets ischild, load_config is a one-line forward to CONF_modules_load_file_ex (6.10). So 6.6 becomes 6.6a-g with each dependency named. Seventeen of the eighteen live index slots belong to a later stratum and the arms answer NULL until their owner lands: that gap is invisible to every symbol ledger, because a field that was never filled is not a symbol, which is the a2d_ASN1_OBJECT defect class one layer down. It is made visible three ways -- the per-slot owner table in docs/PHASE-6-SUBPHASES.md and in the module documentation with the closure rule stated in both, the probe printing libctx.slots.live/.filled/.deferred so the transcript states the scope of its own table, and nothing filled with a placeholder, because the value is a live object of a type a later stratum owns and a one-byte allocation would satisfy the observation while saying something false about the object. An unfilled slot may not be observed either: comparing a missing subsystem is the ledger's business, not a court's. Two further deferrals are named in the code rather than left as absences: context_deinit's ossl_ctx_thread_stop waits for 6.6e, and ossl_do_ex_data_init's per-context registry stays the process-global Phase 3 deferral it already was. The FRF store is recreated: 42 receipts, 84 challenges, 5 claims, graph_verified.
    state state.d277ea2db6f2934071fc81ed3dae9d99e821742661da2c8855dbe43b70eb37be -> state.5856f8075027b3886decc069f9bdb4e7538257aaa1ba8f14bb00dcac5ae826e4
C61  The divergence RT-PARAM reported was not a divergence. courts/phase6/rt_param_probe.c filled a sixteen-byte buffer with 0xaa and handed it to OSSL_PARAM_construct_utf8_string(..., buf, 0), which measures the buffer with strlen, so the terminator was whatever followed the array on the stack. Measured on the committed source: -O0 answers 22 for both sides, -O1/-O2/-O3 answer 16 for both, and the capture the receipt was bound to recorded 24 against 22. The residual a0eaeb5b2946497400a7b63f1d0d78efe4f90ae91ee24ef1e910c7ce19551a7 was the optimizer's frame compared against itself. The probe now terminates its own buffer and observes two declared lengths, so the court compares strlen+1 of a declared object rather than a number a frame produced. Added forensics/tools/probe_hygiene.py: it compiles every courts/phase<N>/*_probe.c against each side at -O0/-O1/-O2, runs each twice, and fails on level drift (uninitialised or out-of-bounds reads) or run drift (nondeterminism no court can compare). It was falsified against the pre-fix text before being trusted and reports both signatures on it, including 22 then 17 from two executions of the same -O0 binary. A sanitizer cannot be used: ASan reserves a terabyte-scale shadow and aborts under the court's own RLIMIT_DATA cap, which stays. The store was recreated from clean, which is what the README already requires for a rebuilt candidate and what a probe edit also requires: the previous store held transcripts from a probe source that no longer existed, and that is why this court's stdout-first-line mutant could not adjudicate. 41 receipts, 82 challenges, 5 claims, all 37 runtime courts isolated on both axes. Also fixed phase6_courts.py declaring GENERATOR as phase5_courts.py, which no gate compared.
    state state.cac3350896083fe525a9e628021df2eb3d42b7eb670dec564a2c8194a6c208dc -> state.d277ea2db6f2934071fc81ed3dae9d99e821742661da2c8855dbe43b70eb37be
C47  RT-PARAM is 1161 observations on each side with zero residuals: the descriptor as a matrix of accessor x width x signedness x type, the setters' NULL-buffer size queries, the string and pointer forms, BIGNUM, dup/merge/free, text allocation and the builder. It found two defects in the new code and one in Phase 5, whose signed encoders accepted five destinations the authority refuses. Phase 5's seal gains a correction section, RT-BN gains 860 observations, and phase_state.py gains the Phase 6 evidence registry that makes run_courts.py run this stratum at all.
    state state.d13b7eabc061c85276030b14eb654fbef4f46fa0686967a514af9befb195a54b -> state.cac3350896083fe525a9e628021df2eb3d42b7eb670dec564a2c8194a6c208dc
C46  56 + 3 + 2 + 20 exports across src/params/mod.rs, dup.rs, from_text.rs and build.rs. Both prototype planes now read every one, after two instrument fixes: fifteen exports returning a struct by value were checked by neither plane, because an unclassified class plane skipped the type plane as well, and a macro_rules scalar push put a metavariable in a type position, which the court refuses by design. RT-PARAM does not exist yet, so the surface is implemented and uncourted and 6.5 stays in progress.
    state state.2c91cb79897ee2f92df8e2f0b3ccc6f03266c6a5a8fa4305132bcec6924d4e4c -> state.d13b7eabc061c85276030b14eb654fbef4f46fa0686967a514af9befb195a54b
C45  Two new Phase 3 courts measure the branch a consumer who never embeds is in. Thirteen zero-length divergences, a CRYPTO_realloc(addr,0) release hidden behind its own NULL return, a candidate-only out-of-bounds read in CRYPTO_memdup, two CRYPTO_set/get_mem_functions dispatch gaps, one invented constant name, and the params.c raise-site macro plane in the generator.
    state state.bfb0e5c1af845a49043f8ce28ad5c6dbfcec3f15833292a131788ef6254e6ea1 -> state.2c91cb79897ee2f92df8e2f0b3ccc6f03266c6a5a8fa4305132bcec6924d4e4c
C44  The ownership atlas is reconciled against the ledgers in both directions, the prototype court judges every implemented export, and the 47 obligations that reconciliation exposed are discharged or handed on
    state state.e37e2612ce18778003bd493480dcb5338c3f108bf61f91ee0ef5bad6f970e415 -> state.bfb0e5c1af845a49043f8ce28ad5c6dbfcec3f15833292a131788ef6254e6ea1
C43  PEM_proc_type and PEM_dek_info implemented and courted; the 25 remaining pem.h exports handed on with the dependency each waits for; open obligations reach zero and the phase-5 seal is rewritten from the ledgers
    state state.a2d15e14bf47e8737740783806df1b10919f4749b3013836b191ee6a80c109cf -> state.e37e2612ce18778003bd493480dcb5338c3f108bf61f91ee0ef5bad6f970e415
C42  SMIME_crlf_copy and i2d_ASN1_bio_stream implemented and courted; SMIME_crlf_copy removed from the Phase 12 hand-off set because its reason was a file rather than a dependency; PEM_write_bio_ASN1_stream handed to Phase 7 for its base64 BIO; the authority unbounded unwind loop recorded as D-MIME-1
    state state.967494a4aa380bf6a345ea55e84fb1e9901f4b0fc94df3a9048bd9a777d4d786 -> state.a2d15e14bf47e8737740783806df1b10919f4749b3013836b191ee6a80c109cf
C41  ASN1_item_print implemented and courth; the EMBED stack-slot defect the court found; the v3_utl.c raise coordinates and the phase 4 to phase 5 hand-off reconciliation
    state state.06d7e5252a13d7ebf03db77fe2b9f15e1776e93adb37cc181713b660e8df098b -> state.967494a4aa380bf6a345ea55e84fb1e9901f4b0fc94df3a9048bd9a777d4d786
C40  Subphase 5.8 half landed: crypto/asn1/bio_asn1.c (BIO_f_asn1, the four BIO_asn1_ prefix/suffix controls) and the BIO_new_NDEF half of bio_ndef.c, with its two prefix and two suffix callbacks. The filter is a state machine, not a wrapper: seven states because each write may have to emit a prefix, a header, some content and then more content on the next call, with a partial-write cursor for the header and a declared length that bounds how much content passes through before a fresh header. Added RT-BIO-ASN1 with 114 observations, and it found one defect, which crashed the candidate. The authority tests the three setup calls as ; I wrote them as , which multiplies by zero on SUCCESS, so BIO_new_NDEF took the error path on every call and freed the support block a second time - the state machine had already handed it to the BIO, whose destroy callback releases it. glibc reported a double free in tcache and the probe dumped core. The lesson is the one this stratum keeps teaching from the other direction: a faithful transcription of three lines would have been right, and the tidy-looking rewrite was wrong. Fixed to the authority shape. Implemented libcrypto exports 921 -> 927; Phase 5 open obligations 37 -> 30.
    state state.5ef38880d6673996c7801ac89d7043d9e6423aac626fa6098879d1e59c8d4458 -> state.06d7e5252a13d7ebf03db77fe2b9f15e1776e93adb37cc181713b660e8df098b
C39  ASN1_str2mask lands with the asn1_str2tag table it depends on. The two other exports of asn1_gen.c, ASN1_generate_v3 and ASN1_generate_nconf, are handed to Phase 11: both take an X509V3_CTX pointer, and ASN1_generate_nconf constructs one through the X509V3_set_nconf macro even on its null-CONF path, so neither can be written without a structure this stratum does not own. The fifty-four-name table lives in this module rather than beside the generator because ASN1_str2mask needs it now and Phase 11 will need the same table; a second copy is a duplicated registry. RT-ASN1-STR grew to 5831 observations with thirty-eight str2mask cases covering accepted names, the DIR special case that shadows the table, the lowercase and mixed-case forms, the two separators, empty and separator-only lists, unknown names before and after an accepted one, the six modifier names that the ASN1_GEN_FLAG range test rejects, and the partial mask a refusal leaves behind. All pass on the first run, which is the first time in this stratum that a module written from a reading of the source needed no correction. Implemented libcrypto exports 920 -> 921; Phase 5 open obligations 39 -> 37.
    state state.6b6aa057e39335f1cca6e8705e88fe15c60b8cf5b0db243627ded409f5e2585b -> state.5ef38880d6673996c7801ac89d7043d9e6423aac626fa6098879d1e59c8d4458
C38  Completed subphase 5.6: a_strex.c lands, so the string surface has no open export left except ASN1_str2mask, which belongs to asn1_gen.c. ASN1_STRING_print_ex and ASN1_STRING_print_ex_fp are one implementation with two sinks, and ASN1_STRING_to_UTF8 is the same tag2nbyte table read for a different purpose. The char_type table is the authority generated charmap.h artifact rather than a re-derivation of charmap.pl, and RT-ASN1-STR is extended to pin it behaviourally: every one of the 256 byte values printed under RFC2253, under ESC_MSB alone and under ESC_QUOTE, plus twenty-three flag sets over thirty string types. RT-ASN1-STR is now 5714 observations and passes. One finding, and it is about the probe rather than the crate: do_dump builds a stack ASN1_TYPE whose value.ptr is the ASN1_STRING, so with DUMP_DER the encoder reinterprets that pointer according to the string type - for BOOLEAN that is the low byte of a heap address, so the authority and the candidate each printed their own address and the comparison was invalid rather than failing. The probe now restricts DUMP_DER to types where the union member really is the string, and the restriction is written down where it is applied. Also fixed two clippy findings on the way (collapsible match, and i2d_ASN1_TYPE taking a const pointer). Implemented libcrypto exports 917 -> 920; Phase 5 open obligations 42 -> 39.
    state state.f0d8841f77ad4b4159c5a699b212bf169331d68da2827ed44291f170fac6935c -> state.6b6aa057e39335f1cca6e8705e88fe15c60b8cf5b0db243627ded409f5e2585b
C37  Implemented 14 of the 20 exports of subphase 5.6: a_print.c (ASN1_PRINTABLE_type, ASN1_UNIVERSALSTRING_to_string, ASN1_STRING_print), a_mbstr.c (ASN1_mbstring_copy, ASN1_mbstring_ncopy), a_strnid.c (the 28-row standard table, the runtime stack that shadows it, ASN1_STRING_TABLE_add/get/cleanup, ASN1_STRING_set_by_NID, the three global-mask accessors) and t_pkey.c (ASN1_buf_print, ASN1_bn_print). Two exports are handed on rather than written: ASN1_add_oid_module to Phase 6 and ASN1_add_stable_module to Phase 11, because both register a CONF module through CONF_module_add, which Phase 4 handed to Phase 6 since only the module registry constructs a CONF_MODULE. Added RT-ASN1-STR: 581 observations, passing after three fixes it found. (1) ASN1_PRINT_MAX_INDENT is 128, not 80 - I wrote 80 from the ASCII line width instead of reading t_pkey.c, and with an indent of 81 the authority writes 81 spaces where the candidate truncated to 80. That is exactly the transcription class D33 forbids and the reason the constant now names its source. (2) ASN1_STRING_set_default_mask_asc must reject MASK: with an empty remainder; I accepted it because strtoul accepts it as zero. (3) The court exposed a Phase-3 decision rather than a Phase-5 defect: OPENSSL_INIT_LOAD_CONFIG was refused with ERR_R_INIT_FAIL because the authority action was said to be non-empty, but the authority reaches CONF_modules_load_file_ex with DEFAULT_CONF_MFLAGS, which carries CONF_MFLAGS_IGNORE_MISSING_FILE, so on a profile with no default config file the step succeeds having loaded nothing. ASN1_STRING_TABLE_get calls it on every lookup, so the refusal made the whole table raise. The flag is now accepted with the load a no-op, and applying a config file that does exist remains a Phase-6 obligation recorded as D86. Also added setvbuf line buffering to the probe, so a probe that dies part-way leaves the observations it did make rather than an empty transcript. Implemented libcrypto exports 903 -> 917; Phase 5 open obligations 58 -> 42.
    state state.59eeed712ec296d8be516d1dc49380b12eda93547de1db3fd169b9725f17355d -> state.f0d8841f77ad4b4159c5a699b212bf169331d68da2827ed44291f170fac6935c
C36  Implemented the 29 exports of the ASN.1 time family (crypto/asn1/a_time.c, a_utctm.c, a_gentm.c) and the three crypto/o_time.c calendar symbols they stand on. The parser is one function, ossl_asn1_time_to_tm, with everything else a wrapper: the four constructors and their year-window choice, the two type guards, the three cmp_time_t answers, the four printers over a memory BIO, the duplicates and the two in-place converters. Added RT-ASN1-TIME: 1071 observations, all matching, on the first run after one defect was fixed. The defect is the reason the court exists: ASN1_TIME_print called the internal three-valued printer directly instead of going through the public ASN1_TIME_print_ex that collapses its -1 to 0, so an unparseable value returned -1 where the authority returns 0. No unit test would have found it - the difference is only observable through the public entry point. Also added src/runtime/time.rs: the glibc struct tm projection the exported signatures are written against, OPENSSL_gmtime over gmtime_r, and the Fliegel and Van Flandern Julian-day arithmetic of OPENSSL_gmtime_adj and OPENSSL_gmtime_diff, with the Julian-day unit tests and the struct layout assertion. Implemented libcrypto exports 871 -> 903; Phase 5 open obligations 87 -> 58.
    state state.6218370ade549f6470b9b1a5c822d6d70ac101fd24a1937aed4462dc979d53d0 -> state.59eeed712ec296d8be516d1dc49380b12eda93547de1db3fd169b9725f17355d
C35  Phase 5.3: the shared DER codec lands in both halves, the 26 primitive item descriptors, the 36 tasn_typ.c wrappers, ASN1_BIT_STRING, ASN1_NULL and d2i_ASN1_UINTEGER. RT-ASN1 goes 644 -> 1306 observations over 0 residuals, phase 5 open obligations 226 -> 155 and implemented[libcrypto] 734 -> 805, moving together as they must. Restructuring the decoder found two defects in the file D76 had landed and neither was reachable from the three wrappers it shipped: asn1_ex_c2i frees the value unconditionally and nulls the callers slot on an allocation failure, where the old code freed only what it had allocated, so a d2i_* into an existing string would have handed back a slot pointing at a half-filled object the authority had destroyed; and asn1_item_embed_d2i raises ASN1_R_TOO_SMALL for len <= 0 before any header is read, where the old code reported a different reason for the same failure. The court then found an ownership contract rather than a bug: asn1_item_ex_d2i_intern ends with if (rv <= 0) ASN1_item_ex_free(pval, it), so a failed decode frees the callers value and nulls the callers slot. RT-ASN1 bsd.keepstate, authority=0 candidate=1, is what produced src/asn1/fre.rs, and it also explains why asn1_ex_c2i nulls the slot after freeing: that null is what stops the item layer freeing the same string twice. The 26 descriptors are compared field by field because ASN1_ITEM fields are readable in asn1t.h, and that found IMPLEMENT_ASN1_TYPE passes 0 and not -1 as the items size, which is the field that decides whether a BOOLEAN is omitted. Reason codes are generated now: gen_err_reasons.py reads 1839 LIB_R_NAME defines over 541 authority headers and cross-checks itself against gen_err_strings.parse_reason_codes before writing, so the two codes D76 had typed wrong become references and that class of mistake is gone rather than fixed once. The subphase plan had 5.4 before 5.3 and reading the file showed the dependency points the other way: ASN1_item_d2i reaches the primitive arms with no template involved, so the wrappers need the primitive path, which is the work 5.3 owns. FRF: run run-openssl-rs-rt-asn1-2053f9dbaa2d8e2fe0ce0fb78259b91735264d11405d6e227cf82d0efff062bf, receipt receipt-run-openssl-rs-rt-asn1-2053f9dbaa2d8e2fe0ce0fb78259b91735264d11405d6e227cf82d0efff062bf-3ce41207fbca8cd851ed8818e2a250452cd9ee16fe63ad06705f7cc977a29f70, challenges 3c70b564a201f55de682d7d9409f542dfeece2dab705faa18b8d895f60d1b2b4 (stdout-first-line on the stdout axis) and 6229f4ca6cc3b646872d56976633d9434ef813307bdc52018b9196faff74e40d (exit-class on the exit axis), claim 414204ce21684e41a0352d4d33a303244ff2fedbe0c6abd6acd52e598b729617 over the 25 runtime receipts plus the ABI, dgst and inventory courts. D77 records it and docs/PHASE-5-SUBPHASES.md corrects the ordering and gains the authority facts this section established.
    state state.ffc65cf51aaa8caf989a94c1cfa21fb53ab84cd2fdea8b7259d5891c31ddbebf -> state.6218370ade549f6470b9b1a5c822d6d70ac101fd24a1937aed4462dc979d53d0
C31  The ASN.1 leaf surface is implemented, wired in and courted: 109 exports, RT-ASN1 at 644 observations with no residuals, phase 5 open obligations 362 -> 226 after D73 hand-offs
    state state.cae66a95a90b21ac673e9b4d9041094b4d051acd5af8496a783b5f067becb2cc -> state.ffc65cf51aaa8caf989a94c1cfa21fb53ab84cd2fdea8b7259d5891c31ddbebf
C30  The prototype court compares the canonical type of every implemented export return and parameter, not only return class and arity; twelve wrong declarations found and fixed; the RT-BN BN_GENCB section added and a thirteenth behavioural defect found
    state state.f48f703c3fc7896f18ac74e6848843bd301f48b25869f6d07598966c16e0b4b9 -> state.cae66a95a90b21ac673e9b4d9041094b4d051acd5af8496a783b5f067becb2cc
C29  GF(2^m) closes BN. src/bn/gf2m.rs implements all fifteen BN_GF2m_* exports — add, poly2arr, arr2poly, mod, mod_arr, mod_mul, mod_mul_arr, mod_sqr, mod_sqr_arr, mod_exp, mod_exp_arr, mod_inv, mod_inv_arr, mod_div, mod_div_arr — as field arithmetic rather than as the authority carry chains: carry-less multiply, shift-and-xor reduction, polynomial long division, and extended Euclid over GF(2)[x] that reports a non-unit gcd as no-inverse rather than as a wrong answer. RT-BN goes 577 -> 633 observations over 0 residuals, and unlike the eleven earlier passes in this stratum both findings were the implementation and not the probe: poly_rem compared the value bit length against the modulus rather than against the field degree, so 0xa5 * 0x57 in the AES field, exactly one bit longer than the field, came back unreduced but plausible; and BN_GF2m_mod_inv raised nothing on an invalid modulus, when the authority reaches that case through BN_GF2m_mod_mul and a caller therefore sees that function INVALID_LENGTH coordinate. Both are recorded with the coordinate, and the probe now compares the error queue after those failures and not only the return value. The non-_arr entry points convert through BN_GF2m_poly2arr and call the _arr ones, so the two spellings are not independent but they are separately observable, and the court drives both, cross-checks them against each other, and checks the field identities a * a^-1 == 1 and (a/b) * b == a. Two divergences are recorded rather than reproduced: D-GF2M-1, the authority blinds BN_GF2m_mod_inv with BN_priv_rand_ex and this does not because BN_priv_rand_ex is Phase 9 and a fixed blinding value is not blinding, so the returned value is identical and the TIMING claim is removed; and D-GF2M-2, the arithmetic is not the authority unrolled carry chains, so no timing claim is made in either direction. Obligation OBL-GF2M-INV-BLINDING is owned by Phase 9 and closes by adding the blinding when RAND exists. Every one of the 201 exports bn.h declares is now implemented or handed to a named later stratum, so Phase 5 open_in_this_stratum falls from 333 to 318: 280 ASN.1 and 38 PEM, which is the whole of what remains. FRF: run 33d75fee81d80354eda5b7f051aefa6dea3eb9084dc6f1fc00ac1ec0a4948f9b, receipt receipt-run-openssl-rs-rt-bn-33d75fee81d80354eda5b7f051aefa6dea3eb9084dc6f1fc00ac1ec0a4948f9b-bbb16ce6b43b3246f0cc7983dc7f9c5b9c3d46ed212defbb6234bce8f286d2b6, challenges 7894d4ef788712701fb9ce57e5573d93b065b8f0de3ee51e33e413e8576f2702 (stdout-first-line on the stdout axis) and 3e8c880cb9f235f254a17298783daca09ba50dee1b005639a62094e6c91801de (exit-class on the exit axis), claim 564889652b055cbffa62df560f376117dffda456ae674120b166a4c5a02a11c5 over the 24 runtime receipts. The seal FRF section was corrected in this change: it named a capture identity from a superseded store generation, and the identity it names now is the clean run above plus the two challenged runs, which are the three capture directories on disk.
    state state.a5011f9ae7e414dd7ad7814c7bf30290fb1bc84ae250838d4d8683ad552b4db6 -> state.f48f703c3fc7896f18ac74e6848843bd301f48b25869f6d07598966c16e0b4b9
C21  The BN surface is closed: every one of the 201 exports bn.h declares is implemented or handed to a named later stratum, and the stratum court now covers the whole of it. Six new modules: mont (BN_MONT_CTX and the Montgomery arithmetic over it, with R = 2^ceil(bits/64)*64 as the authority picks it), recp (BN_RECP_CTX and reciprocal division, whose quotient and remainder the probe checks against BN_div directly because the reciprocal is a speed device and not a different division), primes (the thirteen named primes, whose values are generated from the authority by forensics/tools/gen_bn_primes.py rather than transcribed, and whose five static NIST objects are cached so two calls answer the same pointer the authority returns), nist (the five reducers and the value-based selector), kron (BN_kronecker, Cohen 1.4.10 including the reciprocity sign written the way the authority writes it), and blinding (BN_BLINDING: new/free, the flag and thread surface, the lock, and invert/invert_ex). The court went from 475 to 577 observations over 0 residuals, and extending it found one defect which was again the probe: the first version handed BN_mod_exp_mont a BN_MONT_CTX re-set to a different modulus, and the authority answered 0 for it because a context is bound to one modulus. With a matching context everything agreed on the first run, and two claims the source only suggested are now measured: that BN_nist_mod_* is BN_nnmod against a fixed prime and ignores the field argument it is handed, and that BN_nist_mod_func selects by value rather than by identity. Thirty-one BN exports are handed to Phase 9 with the dependency as the reason: the RAND families, the prime generators, the five Miller-Rabin primality tests, the X931 generators, BN_generate_dsa_nonce, the GF2m square-root and quadratic-solver pair, and the four blinding entry points that re-create the blinding factor through BN_BLINDING_create_param. Fifteen remain open and all of them are GF(2^m): add, arr2poly, poly2arr, mod, mod_arr, mod_mul, mod_mul_arr, mod_sqr, mod_sqr_arr, mod_exp, mod_exp_arr, mod_inv, mod_inv_arr, mod_div, mod_div_arr. Phase 5 is in-progress and derived: 333 open of 1099 covered, 280 ASN.1, 15 BN, 38 PEM. FRF: run 92c14e395079b876c00b21062faaff4d887ddb913419eb73ea6c1efd9e2e17c4, receipt bfc1b9169a88d2e60a682a69612465eb6b520e653097ba3f75719d6f07b8df82, challenge 551ff830a4b500afa06a1c117f842434634e470122b35e0f5cb0c97cafc70301, claim 62ccbb07477191b275df2601447473095452b7b409c94b0d56b8059bab507b43. The prototype court added in the previous change is now a CI gate and reproduces in evidence_determinism: 550 of 610 implemented exports checked, 0 mismatches.
    state state.ebcc96f517c951fc5431bb54f9e43b20a390d8f6c6d8645b10598f83edcf4c1e -> state.a5011f9ae7e414dd7ad7814c7bf30290fb1bc84ae250838d4d8683ad552b4db6
C20  Phase 5 BN arithmetic closed, and the court that closed it found nine behaviours first. RT-BN compiles one C probe against the admitted authority and against the candidate and diffs the transcripts: 45 residuals on its first real run, 0 at closure, over 475 observations. The first run was a probe bug (a freed BIGNUM slot reused through BN_asc2bn, because BN_free does not clear the caller pointer) and that is the tenth time in this project that the probe was the suspect. The 45 were real: BN_bn2hex is byte-oriented and not nibble-oriented (32 of them were one nibble wide), BN_cmp orders by sign before magnitude and negating the magnitude answer inverts exactly the mixed-sign case, BN_asc2bn answers 1 or 0 and not a digit count, BN_usub reports only a limb-count shortfall and otherwise lets the final borrow escape, BN_mask_bits reports a width past the value top limb, the mod family reduces last rather than first so a negative operand contributes its sign, the _quick variants are their own algorithms, a negative exponent is not rejected because the ladder reads the magnitude, and the error queue is part of the answer at the authority own coordinates (all nineteen raising crypto/bn translation units now covered by gen_err_raise_sites.py). One residual was a panic rather than a wrong value: BN_mod_inverse(a,0) reached limbs::rem division-by-zero assertion, which guard_ffi caught into a null return with no error raised because the raise happens after the arithmetic; limbs::mod_inverse now answers None for a zero modulus, which is also the mathematically right answer. The most important finding was a call convention: BN_signed_lebin2bn and its relatives were declared -> c_int while the authority declares BIGNUM *, and they required a non-null ret, so the documented NULL-means-allocate form failed. The atlas recorded the prototype and nothing compared it, so forensics/tools/prototype_court.py now compares return class and arity for every implemented export (resolving both sides through their typedef chains so spelling never reads as shape), with symbols it cannot check named by class and never counted as passes. Its first real run found two more, in this stratum own new code: BN_CTX_new_ex and BN_CTX_secure_new_ex carried a const char *propq that belongs to the EVP constructors and not to these, harmless at every call site on this ABI which is exactly why only a prototype comparison finds it. Recorded: the RAND-dependent exports of this stratum (BN_rand family, BN_generate_prime family, BN_generate_dsa_nonce, BN_BLINDING_create_param, the X931 generators and the GF2m sqrt/solve_quad pair) are handed to Phase 9 with the dependency as the reason; the two ABI_ONLY_EXPORTED symbols the atlas has no prototype for (OPENSSL_DIR_read, OPENSSL_DIR_end) are reported rather than skipped. Phase 5 is in-progress and derived: 391 open of 1099 covered, 280 ASN.1, 73 BN, 38 PEM. FRF: run-openssl-rs-rt-bn-5580515d5fb3a457d26b65acfbb5b448e525df3a596f248f74418b3f55d3fdef, receipt f58c3b404fe7832043ddeca4fa58ad8dbec69b3beed1071a5502e4be7e9350bf, challenge 37815ebd91f5a0fc28e7417539a4706bb0fef1e577fa256c495dfcd5d4d112e9, claim 7648b6178e10d5b50f9dae13fcc0335283737f5abe7ec259d5ea18973c17f69c.
    state state.3b639aa874b366dd8e46bfd6d8f395c89cfa35c9b813f0f5c3ac260c43531c51 -> state.ebcc96f517c951fc5431bb54f9e43b20a390d8f6c6d8645b10598f83edcf4c1e
C19  Phase 5 archaeology and the BN substrate. The stratum owns 513 of the authority exports, measured rather than typed: 201 in bn.h, 274 in asn1.h/asn1t.h and 38 generic PEM entry points, with 580 handed to later strata by declaring header (23 to phase 7, 58 to 8, 12 to 10, 346 to 11, 141 to 12). The families had to be derived because the candidate surface spans 270 distinct ASN.1 type names and prefix lists cannot separate d2i_X509 from d2i_ASN1_INTEGER; typed families are also how the D49/D51 defect class arose twice. The ledger fails closed on an unknown header. The BN substrate is written but not committed and not wired in: making the entry points unsafe extern C cascades into about sixty internal SAFETY comments, and there is no differential court yet, so committing would put symbols in the ABI shell as implemented on unit-test evidence alone. Twelve limb unit tests pass, and three of their expectations were wrong while the implementation was right.
    state state.e0c7044436a5db04f8a67931be2e8532bae6af04741d0789a736157bd367f2ab -> state.3b639aa874b366dd8e46bfd6d8f395c89cfa35c9b813f0f5c3ac260c43531c51
C18  Phase 4 evidence reproducibility: the RT-BIO-DEBUG probe leaked an ASLR-dependent subject address to stderr through the NULL-destination fallback of BIO_debug_callback_ex. The court declares stdout and exit as its axes, and its stdout was already stable and scrubbed, so the claim was never affected; but the capture includes stderr, so every run produced a different evidence identity, and that one court was enough to make the aggregate runtime claim identity unstable. Measured: three re-runs of the court in one store produced three different run ids. setarch -R is refused in both court containers, so ASLR cannot be disabled. The probe now redirects descriptor 2 to a temporary file for that one call, restores it, and reports the scrubbed text as two further observations. Three re-runs now return the identical run id and FRF refuses to re-capture, which is the falsifiable test that it is fixed.
    state state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5 -> state.e0c7044436a5db04f8a67931be2e8532bae6af04741d0789a736157bd367f2ab
C17  Record correction: the Phase 4 ledger hands thirty-one exports to later strata, in five groups, each with a named owning phase and a stated reason. Six to Phase 5 (the ASN.1 prefix and suffix hooks plus BIO_f_asn1 and BIO_new_NDEF), seventeen to Phase 6 (BIO_s_core, BIO_new_from_core_bio and the fifteen CONF module-registry entry points), five to Phase 7 (the digest, cipher, reliable and base64 filters and BIO_set_cipher), one to Phase 9 (BIO_f_nbio_test, whose read and write call RAND_priv_bytes), and two to Phase 12 (BIO_new_CMS and BIO_new_PKCS7). The eleven symbols Phase 3 handed to Phase 4 are recorded on the Phase 3 side as discharged, not as Phase 4 deferrals. No file content changed.
    state state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5 -> state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5
C16  Record correction: the Phase 4 closure change C15 says the ledger hands twenty-two symbols to later strata. The ledger hands thirty-one, and two of the thirty-one are the ASN.1 prefix hooks and CMS/PKCS#7 BIO bindings; the eleven discharged Phase 3 hand-offs are counted on the Phase 3 side, not as Phase 4 deferrals. No file content changed with this correction; the Git commit remains the authoritative record of the diff.
    state state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5 -> state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5
C15  Phase 4 complete: sixteen differential BIO, BIO-adjacent, OBJ and CONF courts with no residual over 3215 observations; the whole CONF reader including both character-class tables; the lhash insertion-order defect the dump text exposed; the ERR data formatting the authority does through BIO_vsnprintf; and a ledger that hands twenty-two symbols to named later strata. The FRF runtime claim is now sensitivity-backed over twenty-three courts.
    state state.6fe2d6a4caa8fde4a5aed8933bcac4e4a7e403eb92165dfc85b56f54dfce9c5a -> state.6362a83c6c38c4083b7e282c2c06129d5e2c180c6f75f473b2f79f8cba98f7a5
C13  Record correction: Gemel names changes by derived order, so the name C9 that Phase 3 closure (change.0321160b...) quotes in its summary is not stable. The placeholder change it means is change.e58167ada98b43af520cd6b8c39c103a30560c406cfd91e6004019fd0b75f326, currently named C11, and it is the change that carries the Phase 3 working-tree operations because it was created first. No content changed; this change exists so the durable identity is recorded in the store rather than only in prose.
    state state.fad064947e65109210abd9e4d2e1a79a9945c6dc6604c8d6c74501c1d5f1045c -> state.6fe2d6a4caa8fde4a5aed8933bcac4e4a7e403eb92165dfc85b56f54dfce9c5a
C10  Phase 3 complete: seven differential runtime courts with no residual, generated ERR reason tables and raise-site coordinates, load-gated string visibility, fail-closed obligation ledger. Supersedes C9, which was created accidentally by a command-line probe of the claim-kind enum and carries only a placeholder claim.
    state state.fad064947e65109210abd9e4d2e1a79a9945c6dc6604c8d6c74501c1d5f1045c -> state.fad064947e65109210abd9e4d2e1a79a9945c6dc6604c8d6c74501c1d5f1045c
C11  p
    state state.46cf9bcff7dad1b49b613c87d2458196666c4d2e787c76ea29702255e3913716 -> state.fad064947e65109210abd9e4d2e1a79a9945c6dc6604c8d6c74501c1d5f1045c
C6  Phase 2.1 structural closure: dynamic contract, exact symbol comparison, evidence-derived phase state
    state state.ba6b30cf8a7741640b349cf65bff4a30247994151265f119bd5f2d6a4073a42b -> state.46cf9bcff7dad1b49b613c87d2458196666c4d2e787c76ea29702255e3913716
C5  Phase 2 complete: distribution shell, ABI matrix, substitution, constants, FRF ABI court
    state state.c86c4178c1597a82a06176665f752d3aa9107c30b5be2178f67ba5d5f4526621 -> state.ba6b30cf8a7741640b349cf65bff4a30247994151265f119bd5f2d6a4073a42b
C2  Phase 1 foundation closure: differential atlas, completeness, FRF sensitivity-backed claim, FFI boundary
    state state.85d1e8ede88fb9f355d3d7e01f71483f9b28f0a857898a706a5fe5a3099a44f2 -> state.c86c4178c1597a82a06176665f752d3aa9107c30b5be2178f67ba5d5f4526621
C1  Phase 0 constitution and Phase 1 archaeology atlas, evidence-bound
    state (initial) -> state.85d1e8ede88fb9f355d3d7e01f71483f9b28f0a857898a706a5fe5a3099a44f2
```

## Checkpoints

* `K1` — `checkpoint.2a1ca2279b999e7740976a4958d6d6bb66e5312203ed9d15816d340a38073328`
* `K10` — `checkpoint.51ac8d8e9e91d360fe1bb94630661f0fd4071dd844baa1cd42c82b4e1504ba08`
* `K11` — `checkpoint.843b3f72da905eec1a0fc67a16ee8f1dc4811f86f51dfa75ae68a4d0c88ac4c6`
* `K12` — `checkpoint.3d4d0ddafc52a6635d9cf963b6fd27888133ca39ccc83243082477e7ddfaec45`
* `K13` — `checkpoint.f8f1061c5592c5dc801b31439f949ed21470126cd32b0f320fea3bf561acba55`
* `K14` — `checkpoint.c3c9dd019cd987d9753cda5e521b8993decd99c01b97fcebeef888727f56512e`
* `K15` — `checkpoint.be15113912c9856c707bdd4c9317edc7c750663fd96e76e1a5b5e2b9a4420c3d`
* `K16` — `checkpoint.6efde8d368887ea3895c564c33f8cc2ea84b211d56971fb3055ae89c576c10e2`
* `K17` — `checkpoint.5fd6e1426ad1bf8934578ecdb7020b8bf72c4ca401136e0884da0b7c0f4b22c6`
* `K18` — `checkpoint.945a0214a4146448bc80b811fa2d65ffa531a1d49396b93cc0bbf715e26dba62`
* `K19` — `checkpoint.73c21457ed3266796156fb1e55e8a60bc559a8d9398b610297c336bb30b956fd`
* `K2` — `checkpoint.67a75f9a16d008e6e5aec0984c93dfc7549bd7a33708b909fbf6424b04fcb4a6`
* `K20` — `checkpoint.528dbaef805d3e05304fbc9ae6e8bee5c1a5bb2e75486f2998933b875732bed5`
* `K21` — `checkpoint.6933308364494b57c6c48ab9e4246d741e27a9e013ea2bfac9ce0dfda8fe2f48`
* `K22` — `checkpoint.5c49d38a618be5dfb2a1d350b73a7f13b5dc9b4f2629a36ce4bf882ad814752f`
* `K23` — `checkpoint.4d121775b53e8369dd0457ad230f82a3b375db10e9df7077ad4d564dcdc756fd`
* `K24` — `checkpoint.1a2e3a5565633ce851d7dadbcbc9c5cd2be4cf8d61b6463f45b54cc7fa39ec8a`
* `K25` — `checkpoint.72f7fa3b2028e181c38bcb2d904f665cad991c15dfbe446629b3aee4147edc15`
* `K26` — `checkpoint.4e8675a95b17554b5113e18653828e79c7c859cc37f7c3a2183a9c4864041d34`
* `K27` — `checkpoint.cd670f06b0cd5409439a863fffb94c388c24bf3617eafdbf012d833da79a7e13`
* `K28` — `checkpoint.5f82da003df177c43703c768867caf5616b5bcb1a85f3376494d2730c74abdb9`
* `K29` — `checkpoint.a3713ea56466fccba028c053d2e9f17756e5067eec4a78fb90347aed6d8aad10`
* `K3` — `checkpoint.b1516eb6364ad075785911cb204a75a6e1b83b08c1a7ca39a2de6e3983dc9aed`
* `K30` — `checkpoint.76a96561abab60000acae0b6e28920a3bb935d6b9439dcc8625d6088e944f7dc`
* `K31` — `checkpoint.2cd5a115653b46223c9f08a5fde8e27816bca65403178271723fed449cc83585`
* `K32` — `checkpoint.762086a51a9fb6a0560b5334da2fe6afc6742b9b378e090fe0b5ba2f998812d3`
* `K33` — `checkpoint.78b36f5a69bdf87eee41a35af9d1d98747954289da20f803a1a48608ab264b31`
* `K34` — `checkpoint.a2096945c7a295a6f4b0ce77afd38ff1651b2167d49ea198dc1085892334a3a8`
* `K35` — `checkpoint.5c340ead0989bb79b5832ae7d3d0bf1dffad4e5043a9089f397e71e9391d0fea`
* `K36` — `checkpoint.b2246933fc4896371a88f1b965fb27f01bc107a4fbcaecc3790e219edc435416`
* `K37` — `checkpoint.ccf6f582e8b111b58ff7bb1c67bc2fcc36ba74914f894179a4c6f45c27eb1f28`
* `K38` — `checkpoint.6e5ee6c4f782c2272a2cbd1dc15f6d70bd313e8f98d3cfa232e50b4bf14b5711`
* `K39` — `checkpoint.d1a32fd0ea02c2df1079a6dcf7b47af88efde2b479a94aae7a6b8d1a7f2b0e18`
* `K4` — `checkpoint.1bde75b37e1ca3972037c29cbd3ba5291079544436db9176a82f097a6bf832fe`
* `K40` — `checkpoint.c7857ce4ca595f5667a7592269f14f6be3e1a3a3767737f4a0b684cef19360a8`
* `K41` — `checkpoint.99e7f4d439c3839c698aa19df0b480fa6fe6708de836e638c1877d1355a005a3`
* `K42` — `checkpoint.083cbb4e2a093c08240db9c85cacbc7a21635610de0395307cfd286961b7f608`
* `K43` — `checkpoint.a9076ca84028a43f4f458eed2de258e774f3ebc572481b4d2d82eb400c0125ce`
* `K44` — `checkpoint.a830a112b51e121e29dac90095960d71f262e0d6cd43b191c7795c7873421c0b`
* `K45` — `checkpoint.f03310604dc2fe42b916037ecf6fb3909bb6115ded3083d731e42571b932abef`
* `K46` — `checkpoint.7acbe5053cfa6dcb35a807097157f6cfc0696c11f60b0d2eb569e0fd58cf7537`
* `K47` — `checkpoint.22d001dc15a2fbe7d993f7f2a44649b5f4624ba9c2d12b1290b3619dc27b4206`
* `K5` — `checkpoint.6b0d12f1ecf380c0808bc95675222fbed7256f99bc8e9f475a0ec2693f804a0a`
* `K6` — `checkpoint.8958092650197b473c5b00d9c0de22e075c4765efc2e8a4ab9d452ffae97cf61`
* `K7` — `checkpoint.0c5d62d5f78d2c4ebdc7174affd4464f04d1315708e5a87962cded63439818d3`
* `K8` — `checkpoint.7eb3dba97cbf20f2b34d14cce7e93bc2171ebd0d7ee66d1f7508cc6e199567de`
* `K9` — `checkpoint.60105b4c8189d2668c48e173fe2c92c0ddf76160caae24efecc707bd576f506f`

current: `checkpoint.22d001dc15a2fbe7d993f7f2a44649b5f4624ba9c2d12b1290b3619dc27b4206`

## Note: derived names are not identities

Gemel names changes by derived order, so a name quoted inside one change's
summary can be renumbered later. The Phase 3 closure change
(`change.0321160b791013c5904bb61fe65b4932da674bdbc7f1043b50e2e4369544c227`)
says it supersedes `C9`; that name no longer exists in the store. The change it
means is
`change.e58167ada98b43af520cd6b8c39c103a30560c406cfd91e6004019fd0b75f326`,
currently named `C11`: a placeholder created by a command-line probe of Gemel's
claim-kind enum, which — because it was created first — is also the change that
carries the Phase 3 working-tree operations. The correction
(`change.8423b2c8fe75201fb070e0b21df03d122c8675cde393c9b2d88d9ea1800ee5de`)
records that mapping in the store rather than only in prose. No file content
changed with it; the Git commit is the authoritative record of the diff.

## Open residuals at this boundary

```
open [medium] the three CT-* courts (CT-DIGEST, CT-CIPHER, CT-ML-DSA) are vector-driven and are not declared as FRF runtime courts, so no receipt covers them; their evidence is artifacts/phase8/COURTS.json
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the two mutant residuals the post-fix challenge produced are open by design -- a mutant's divergence is the challenge's evidence -- and no claim admits them
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the pre-fix capture, receipt and residual remain in .frf with their original content addresses; they are evidence of what the earlier tooling observed and are not evidence for the corrected claim
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the two mutant residuals the fresh challenge produced are open by design -- a mutant's divergence is the challenge's evidence -- and no claim admits them
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the admissible claim over this stratum is therefore still 642866a38904cb4122fa40f345b4a6df181f0439b6b729063349fed203ae107f, binding rt-ec by its exit class alone; the corrected both-axes claim is pending the disposition above
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] the pre-fix residual e343c18e1484c4fd8394887b6ead5a9797ae6bad6f777d6aa0747df4f15fb808 remains open on RT-EC's stdout surface and blocks a both-axes claim; the corrected claim cannot be emitted until a human states one of the eight dispositions with a reason
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the four CT-* courts the plan names but whose corpus has not landed (CT-RSA, CT-DH, CT-DSA, CT-EC) are recorded in forensics/tools/phase8_courts.py's PENDING_CORRECTNESS_COURTS, not registered as passing
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the thirty mutant residuals the challenge records produced are open by design -- a mutant's divergence is the challenge's evidence -- and no claim admits them
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the three Phase-8 correctness courts CT-DIGEST, CT-CIPHER and CT-ML-DSA carry no FRF receipt, because they are vector-driven and have no authority transcript for the declaration to name
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] RT-EC's stdout observation short.tail is an uninitialised stack read in courts/phase8/rt_ec_probe.c, so the FRF real run raises one residual there and the compiled claim narrows the rt-ec premise to its exit axis; the probe is not edited here, so the repair is a follow-up rather than discharged
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a passing court is a differential result over the arms that court drives; the arms no probe can reach are listed in each probe's own header rather than implied to be covered
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the legacy cipher and digest contents are absent, so EVP_get_cipherbyname('DES-CBC') and its siblings answer NULL where the authority's table would find a static; a divergence of distribution, named per symbol in 7.3g's table
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] phase 8's method tables make three refusal-shaped answers for the twelve legacy types (D-PKEY-AMETH-1, D-PKEY-AMETH-2), and the names that would answer them are withheld rather than stubbed
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] the 244 deferred symbols are unimplemented and no court calls them; each names its stratum and callee in the ledger and in the gate's blocking census
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] Phase 7 has no FRF court declarations and therefore no receipts: its claim-bearing evidence is artifacts/phase7/COURTS.json plus the differential pipeline, where Phases 3-6 each have a compiled claim
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the probe publishes no algorithms, so OSSL_PROVIDER_query_operation returns a marker and unquery receives it back. What a real algorithm table looks like, and what the fetch does with it, is Phase 7 business and is not observed here
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OSSL_PROVIDER_available is given a name the probe controls and the fallback name only after fallback loading is disabled. A name that resolves through the config file is not observed, because OPENSSL_INIT_LOAD_CONFIG is reached but this build openssl.cnf does not register a provider
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ossl_provider_default_props_update walks the store child callbacks and calls the global-props one. The stack is empty until 6.8e, so the walk is written and observed only as a successful no-op; the callback invocation itself is 6.8e verification
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ossl_provider_dso, ossl_provider_set_operation_bit and ossl_provider_test_operation_bit have no caller in this build: the first is asked by the method stores in Phases 7 to 10 and the other two by the fetch that marks an algorithm as seen. All three are written and carry a dead-code allowance naming their owner, which is a coordinate and not a tested path
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ossl_decoder_cache_flush is the one bridge whose absent-cache answer is 0 rather than 1, and both activation callers discard it. The asymmetry is deliberate and pinned by a unit test, but nothing observes a cache that exists, so the flush itself is Phase 7 verification
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] create_provider_children walks a callback stack that only 6.8e can push into, so it is written as a function that asserts the stack is empty and answers 1. If 6.8e ever registers a callback without adding the walk, the assertion fires instead of children silently not being created, which makes the invariant checked rather than assumed but does not make the walk tested
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the random_bytes guard pair is not reproduced: ossl_rand_check_random_provider_on_load and _on_unload are Phase 9, and the authority guard is prov random_bytes != NULL, which is set only by provider_init walk when a provider publishes OSSL_FUNC_PROVIDER_RANDOM_BYTES. The guard is reachable for such a provider and is skipped in both activation functions. ossl_provider_random_bytes itself is written, because the pointer and the call are this stratum; only its callers are Phase 9
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] provider_activate_fallbacks is entered only in its DISABLED state. The candidate cannot load default, base or null because the predefined table three init pointers are 7 and 8 work, so provider_init takes the module branch and DSO_load fails; entered with the flag set, the walk would answer 0 where the authority answers 1, and ossl_provider_doall_activated and OSSL_PROVIDER_available would inherit that. A unit test pins the failure so the residual is retired deliberately when those entry points land
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] nothing observes that the provider store is the FIRST slot object built, because construction order is not visible through any export and this profile disables the allocation-failure injection that would expose the cascade; the order is reproduced from the source and corroborated only by the deinit side being observable in principle
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the construction and teardown orders are now the authority for every landed slot, but the interleaving with the unlanded ones cannot be checked until they land, so the P1/P2 constraints are satisfied only vacuously for the positions that are still gaps
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] the three predefined providers resolve as builtins with no init function until 7/8 supply ossl_default_provider_init, ossl_base_provider_init and ossl_null_provider_init, so 6.8c provider_init would take them down the DSO branch; recorded in the table and unreachable before 6.8c
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] DSO_global_lookup and DSO_dsobyaddr hand their names to the loader, so their failure text is glibc and not OpenSSL; the probe compares NULL-ness and deliberately not the text, and a NULL name is not probed at all because dlsym treatment of it is the loader to define
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] dlfcn_load saves and restores errno around a successful dlopen, which the authority does explicitly, but the probe does not read errno, so that preservation is reproduced from the source rather than measured
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the RTLD_GLOBAL branch of dlfcn_load is compiled but not exercised: promoting the library under test to the global scope would change symbol resolution for the remainder of the probe process, so a load with DSO_FLAG_GLOBAL_SYMBOLS is observed only for its success and its flag word
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a successful DSO_bind_func is compared only as NULL or non-NULL; calling through the resolved void(*)(void) is not something a probe can do, so what a resolved symbol does is not this court business
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a DSO path is necessarily a different string on the two sides, so the court compares the size contract and never the value: a defect that returned the wrong module path would be caught, one that changed only the spelling would not
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] OPENSSL_atexit pins its handler with DSO_dsobyaddr and is 6.6e-ii work, so the one consumer that needs the loader cannot be observed yet; DSO_dsobyaddr itself is courted, its caller is not written
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] DSO_global_lookup and DSO_dsobyaddr hand their names to the loader, so their failure text is glibc and not OpenSSL; the probe compares NULL-ness and deliberately not the text, and a NULL name is not probed at all because dlsym treatment of it is the loader to define
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] dlfcn_load saves and restores errno around a successful dlopen, which the authority does explicitly, but the probe does not read errno, so that preservation is reproduced from the source rather than measured
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the RTLD_GLOBAL branch of dlfcn_load is compiled but not exercised: promoting the library under test to the global scope would change symbol resolution for the remainder of the probe process, so a load with DSO_FLAG_GLOBAL_SYMBOLS is observed only for its success and its flag word
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a successful DSO_bind_func is compared only as NULL or non-NULL; calling through the resolved void(*)(void) is not something a probe can do, so what a resolved symbol does is not this court business
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a DSO path is necessarily a different string on the two sides, so the court compares the size contract and never the value: a defect that returned the wrong module path would be caught, one that changed only the spelling would not
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] OPENSSL_atexit pins its handler with DSO_dsobyaddr and is 6.6e-ii work, so the one consumer that needs the loader cannot be observed yet; DSO_dsobyaddr itself is courted, its caller is not written
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the ctype table is generated but the authority's source tree is not committed, which is also why gen_err_raise_sites.py is in neither the determinism nor the portability list; that gap is still open and needs both tools changed together
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ossl_prop_defn_set's unwind path releases the block whether or not the table kept it, which is a double free if the insertion succeeded and then reported an error; reproduced structurally and unreachable unless an allocation fails
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] the generated ctype table's strong tier cannot run on a runner without the authority's source tree, so CI checks it against the committed artefact instead; a change to the authority would not be caught there, only in the court
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] the property engine has no exported symbol, so neither the grammar nor the cache can be reached by any differential probe; their behaviour becomes observable at 6.8, where a provider fetch applies a query to a candidate set, and is corroborated at 6.12 by an independently written provider
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the reason-symbol resolver now depends on the committed authority source tree for include/internal/propertyerr.h, which is not installed; a future reason family in a third location would need the same treatment
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the definition cache's element destructor releases a list through ossl_property_free, which accepts NULL, so the path is exercised only with a NULL list until 6.7b stores one
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the candidate's global-properties holder omits the authority's no_mirrored bitfield, whose reader is provider.c's and 6.8's, so the two structures differ by four bytes of trailing padding; nothing in this stratum reads or writes the field
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [medium] the property grammar itself -- ossl_parse_property, ossl_parse_query, ossl_property_match_count, ossl_property_merge and the negative-selection rule -- is 6.7b's, and no court can reach it because the engine has no export; its evidence must be a provider fetch at 6.8 and the third-party provider court at 6.12
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the method, the globals block and the handle live in the candidate's own sections rather than in the authority's, so their numeric addresses differ; what is compared is the identity contract -- one stable address per process, stability across calls and difference between contexts -- not the value
    class: platform_divergence
    persistence: 0 descendant change(s)
open [low] the NULL-globals arms of the constructor and of the seven operations are unreachable through the public API because context_init fills slot 17 eagerly, so they carry no court observation and no claim
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OSSL_LIB_CTX_new_from_dispatch(handle, NULL) faults in the authority and returns a usable context here; registered as D-BIOCORE-3
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] BIO_free of a core BIO whose context supplies no BIO_free callback faults in the authority and answers through the candidate's guard here; registered as D-BIOCORE-2
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] a table that supplies read_ex or write_ex without BIO_up_ref faults in the authority and answers NULL here, so the constructor's answer for such a table is not claimed; registered as D-BIOCORE-1
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] ossl_namemap_stored does not pre-populate from the legacy OBJ_NAME database or the EVP_PKEY_ASN1_METHOD set, so a provider-registered name gets number 1 where the authority continues after the legacy names; deferred whole to Phase 13 because the RSA-PSS block alone would make the emptiness guard false
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OSSL_SELF_TEST_oncorrupt_byte with a NULL bytes and a refusing callback answers 0 where the authority writes through the NULL
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] nothing invokes the indicator callback; the code that reports an approved or non-approved state is provider-side
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OPENSSL_atexit needs DSO_dsobyaddr to pin its handler's object and waits for 6.9
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OPENSSL_thread_stop and OPENSSL_thread_stop_ex need the per-thread event-handler table in crypto/initthread.c and are 6.6e-ii; context_deinit therefore does not yet call ossl_ctx_thread_stop
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the thread slot's condition variable is created and released but nothing waits on it; the thread pool that signals it is not a Phase 6 subsystem
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] each OSSL_LIB_CTX has its own ex-data registry in the authority; this crate has one process-global one, which src/runtime/ex_data.rs records as a Phase 3 deferral and this subphase does not change
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] context_deinit does not call ossl_ctx_thread_stop, because no per-thread object can exist until 6.6e registers threads against a context
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] seventeen of the eighteen live OSSL_LIB_CTX index slots answer NULL until their owning stratum lands; each slot's owner and the closure rule are in docs/PHASE-6-SUBPHASES.md, and no symbol ledger can see them
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the runtime-court count in forensics/frf/README.md is prose rather than a projection and said 25 while the store held 37; it is corrected, not derived
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ASan is refused by the court's OOM protection, so the level-differential method is a substitute for a sanitizer rather than equivalent to one
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] probe_hygiene.py detects frame dependence, not out-of-bounds reads that land deterministically; a probe can read a fixed wrong location and still agree at every level
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OSSL_PARAM_BLD_push_BN_pad with a negative BIGNUM leaves the value bytes uninitialised because BN_signed_bn2native refuses the recorded width; the refusal is compared, the bytes are not
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] OSSL_PARAM_print_to_bio answers 0 for a NULL array where the authority dereferences it, and the probe records the boundary rather than the behaviour
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] OSSL_PARAM_merge sorts stably where the authority's qsort is unspecified among equal keys within one list
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] setbuf_fromparams is reproduced from the two WPACKET operations it uses rather than from crypto/packet.c, which belongs to Phase 7
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] OSSL_PARAM_print_to_bio answers 0 for a NULL array where the authority dereferences it
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] OSSL_PARAM_merge sorts stably where the authority's qsort is unspecified among equal keys within one list
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] setbuf_fromparams is reproduced from the two WPACKET operations it uses rather than from crypto/packet.c, which belongs to Phase 7
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [medium] RT-PARAM does not exist, so no differential court observes the parameter surface and 6.5 cannot close on the rule in D73
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the zero-length and realloc-zero unit tests were deleted rather than repaired: the test harness shares one process and the allocator branch is chosen once
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [low] CRYPTO_malloc allocation failure raises ERR_R_MALLOC_FAILURE and cannot be exercised in the venue without inducing OOM, which the court forbids
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the authority faults writing through a NULL freeptr in CRYPTO_aligned_alloc and its _array sibling; the candidate answers NULL and the boundary is recorded rather than compared
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] seven implemented exports are declared in headers the authority does not install, so the Phase 1 atlas has no prototype for them; their ABI is still proved by the Phase 2 loader court
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the 22 ASYNC_* exports are deferred to Phase 13; the authority in-tree callers are the async engines and the SSL async API, and OPENSSL_NO_ASYNC is not defined in this profile
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] five Phase 3 exports are deferred to Phase 6 with the dependency named: the two libctx thread-count accessors, the thread-stop pair, and OPENSSL_atexit whose DSO pinning this profile compiles in
    class: verification_gap
    persistence: 0 descendant change(s)
open [medium] OPENSSL_info answers NULL for the build-dependent codes where the authority answers a build-tree path, the same divergence already recorded for CONF_get1_default_config_file, reached by a second route
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] two conf_ssl_* exports index a store that nothing populates until the ssl_conf CONF module runs, which needs CONF_module_add in Phase 6.9
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] four COMP_* exports take a live COMP_CTX and no consumer in this profile can obtain one, because every factory answers NULL and libssl returns entries with a NULL method
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the 25 pem.h exports handed to phases 7 and 11 remain unimplemented
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the authority i2d_ASN1_bio_stream unwind loop does not terminate for a callback returning a detached BIO, and the candidate stops
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] the 27 pem.h exports and PEM_write_bio_ASN1_stream and i2d_ASN1_bio_stream remain unimplemented
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] i2d_ASN1_bio_stream in asn_mime.c remains open pending a check for Phase 12 entanglement
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the NDEF streaming happy path needs a caller-declared item with an ASN1_AUX callback; the probe declares one, and the in-tree items that do this are CMS and PKCS7, which are Phase 12
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] a refused name in ASN1_str2mask leaves the mask accumulated by the earlier names; reproduced and courted, and it is the authority behaviour rather than a defect
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [medium] ASN1_generate_v3 and ASN1_generate_nconf are handed to Phase 11, so no caller can build an ASN1_TYPE from a string until then
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [low] X509_NAME_print_ex and X509_NAME_print_ex_fp are the other half of a_strex.c and belong to Phase 11, so do_name_ex is not written here
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the char_type table is the generated artifact rather than a re-derivation of charmap.pl; the probe pins it through behaviour over all 256 byte values under three flag sets
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [low] ASN1_STRING_print_ex with DUMP_DER on a non-character type encodes the string pointer reinterpreted as that type value, so the output is address-dependent and no probe can compare it; the restriction to comparable types is stated in the probe
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the string-table stack is reached through an AtomicPtr where the authority uses a bare pointer and documents that its own sort is unsynchronised; the observable contract is the same and the crate avoids a mutable static
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [low] ASN1_STRING_print_ex, ASN1_STRING_print_ex_fp, ASN1_STRING_to_UTF8 (a_strex.c) and ASN1_str2mask (asn1_gen.c) remain open in this subphase
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ASN1_add_oid_module is handed to Phase 6 and ASN1_add_stable_module to Phase 11, so neither registers its CONF module until then
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [medium] OPENSSL_INIT_LOAD_CONFIG succeeds without loading a config file, so a config file that exists is not applied, and a stbl_section entry in it is not visible to ASN1_STRING_TABLE_get; the loader needs OSSL_LIB_CTX and the module registry, both Phase 6
    class: contract_mismatch
    persistence: 0 descendant change(s)
open [low] the crate answers a null struct tm destination or a null from/to in OPENSSL_gmtime_diff with the failure value where the authority faults; recorded in SECURITY_DIVERGENCE_POLICY
    class: semantic_divergence
    persistence: 0 descendant change(s)
open [low] the time family raises nothing on a parse failure, so ASN1_TIME_adj raising ASN1_R_ERROR_GETTING_TIME is the one raise site and it needs a libc gmtime_r failure to reach; recorded but not courted
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the offset branch applies OPENSSL_gmtime_adj only when the destination is non-null, so a value whose offset would move the Julian day below zero is accepted by the checker and refused by a fill; reproduced and courted, not a defect
    class: expected_mismatch
    persistence: 0 descendant change(s)
open [low] the item descriptors size fields are compared through the probe rather than through a generated ABI constant court, because struct ASN1_ITEM_st is not in the ABI-LAYOUT aggregate set the phase 2 probe measures
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] ABI-PROTOTYPE cannot check the 36 tasn_typ.c wrappers or the 26 *_it accessors: DECLARE_ASN1_FUNCTIONS generates their declarations, so the atlas header extraction does not see them and the prototype court counts them as generated rather than checked
    class: verification_gap
    persistence: 0 descendant change(s)
open [low] the 155 remaining phase 5 obligations are unimplemented: 128 ASN.1 (the template interpreter, 14 *_it descriptors, the time accessors, the string masks and printing, ASN1_TYPE, the NDEF BIO bridge, asn1_d2i_read_bio) and 27 PEM
    class: verification_gap
    persistence: 0 descendant change(s)
next: --cursor 1789442941860:residual.65ec94405d626e358c85cad0129a2a6a2ca96fc55e93aa1a43880272c4e03cc4
```
