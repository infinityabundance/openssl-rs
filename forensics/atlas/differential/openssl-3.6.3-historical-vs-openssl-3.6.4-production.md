# Differential atlas — `openssl-3.6.3-historical` → `openssl-3.6.4-production`

Generated by `forensics/tools/atlas_differential.py`. `added` means present
in the **to** authority and absent in the **from** authority.

## Movement

| plane | movement |
|---|---|
| `abi_layout_movement` | 0 |
| `cli_movement` | 0 |
| `declared_declaration_movement` | 2 |
| `provider_algorithm_movement` | 0 |
| `symbol_movement` | 0 |
| `total` | 2 |

## Symbol planes

| library | added | removed | version changed |
|---|---|---|---|
| `libcrypto` | 0 | 0 | 0 |
| `libssl` | 0 | 0 | 0 |

`functions`: common 7485, added 0, removed 0 — **identical across the two authorities**

`typedefs`: common 1162, added 0, removed 0 — **identical across the two authorities**

`structs`: common 465, added 0, removed 0 — **identical across the two authorities**

`enums`: common 4, added 0, removed 0 — **identical across the two authorities**

`variables`: common 2, added 0, removed 0 — **identical across the two authorities**

`macros`: common 16803, added 2, removed 0

## Other planes

- abi layout changed aggregates: 0
- provider algorithm classes with changes: 0
- cli option changes: 0
- corpus `fuzz`: identical=False, added=0, removed=0
- corpus `providers`: identical=False, added=0, removed=0
- corpus `test`: identical=False, added=16, removed=0
