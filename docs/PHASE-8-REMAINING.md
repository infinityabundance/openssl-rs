# Phase 8 remaining (generated)

Generated by `forensics/tools/phase8_remaining.py` from `forensics/phase8-obligations.json`,
which is authoritative, and §2's subphase table in
`docs/PHASE-8-SUBPHASES.md`. This document is a **projection** of that
ledger: every count and every symbol name below is read from it. **Do not
edit by hand.** Nothing here is a parity claim; see `docs/PARITY_MODEL.md`.

## Totals

Read from the ledger's `body.counts`.

| quantity | count |
|---|---|
| owned | 786 |
| implemented | 786 |
| deferred to a later phase | 0 |
| open in this stratum | 0 |

The identity `owned = implemented + deferred + open` is `786 = 786 + 0 + 0`, which holds.

## The merge gate — what Phase 8 owes to Phase 9

Every row of the ledger's `deferred` list, grouped by the phase that owns
it and sorted by symbol. This is what a later landing must discharge before
this stratum can be complete.

| symbol | declaring header | owning phase |
|---|---|---|

**0** export(s) are deferred, to Phases . A
stratum cannot be complete while any export it owns is neither implemented
nor handed to a later phase, and these rows are the ones a later
Phases  landing discharges.

## The work Phase 8 still owns

Grouped by subphase, in numeric order. `open` counts the ledger's `open`
rows whose `module` is that subphase's module, so a subphase with no open
row is not listed. The `owns`, `courts` and `depends on` columns are §2's
table, verbatim.

| subphase | owns | open | courts | depends on |
|---|---|---|---|---|

Total open symbols listed below: **0**; the ledger's
`open_in_this_stratum` is 0.

## The subphase order

The plan records the subphases that still hold open work in this order:



`docs/PHASE-8-SUBPHASES.md` §2 records this order, and states that each
row's dependency column was read from the authority's calls rather than
from the export list — the method D114, D118 and D122 established. The
`depends on` column above is that reading, verbatim.

## What only checking the export ledger misses

This document projects `forensics/phase8-obligations.json`, and every row
of that ledger is an **export**. Cross-stratum *internal* names — a helper
a module references that is not an export — are recorded in a different
place: `forensics/prerequisites.json`'s `deferrals` (7 rows, 0 of which name Phase 8 as
owner), and
`forensics/atlas/prerequisite-gate.json` is the generated view of them.
A reader who only checks the export ledger has not seen that half of the
picture.
