# Pin the toolchain. The court image installs this exact version
# (docker/openssl-rs-court.Dockerfile); builds and courts run only in the court.
# See docs/REPRODUCIBILITY.md.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
