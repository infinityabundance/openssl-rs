//! Phase 3 core runtime — `OPENSSL_LH_*`, the open-addressing hash table every
//! later subsystem's lookup structure is built from.
//!
//! Placeholder: this module is filled in by the `lhash` work item. It defines no
//! symbols yet, so the `OPENSSL_LH_*` entry points remain `SCAFFOLDED` in the ABI
//! shell.
